import { app } from '../app-context.js';
import {
  ACH_FMT, BACKFILL, BACKFILL_GROUP_KEYS, BACKFILL_GROUP_OF, BACKFILL_GROUPS,
  BASE_DOCS_URL, BBGL_ERROR_CODE, BS_STAT_ROWS, compareVersions, CONSTANTS, ECAN_LOG, ECSTASY_LOG, ENERGY_LOGS, ENERGY_PARAM,
  EX_OD_LOG, GAME, GYM_TIERS, HAPPY_LOGS, ITEM_GROUP_LABELS, ITEM_LOG_META, ITEM_LOGS, KEYS, LAYOUT, LSD_OD_LOG,
  MSG_CLIPBOARD_DENIED, MSG_KEY_FORMAT_INVALID, MSG_KEY_NETWORK_ERROR, MSG_SYNC_NETWORK_ERROR, MSG_SYNC_QUOTA,
  OD_LOGS, r2, SCRIPT_VERSION, STAT_ENHANCER_PARAM, STAT_HAPPY_PARAM, STAT_KEYS, STAT_LOGS, SYNC_FROM_BUFFER,
  TORN_KEY_ERROR_MAP, TRAIN_ENERGY_PARAM, TRAIN_LOGS, WIPE_BELOW_VERSION, XANAX_LOG, XANAX_OD_LOG, ZERO_BREAKDOWN,
  bbglError, tornKeyErrorText
} from '../core/constants.ts';
import { Log, Perf } from '../core/log.ts';
import {
  ALLOWED_CONFIG_KEYS, TAB_ID, calendarState, dom, graphState, historyCache, lastButtonLocation, layoutObservers,
  refreshClickLog, runtime, saveConfig, saveViewState, setHistoryCache, setLastButtonLocation, setTopCeiling, setViewState,
  topCeilingCache, topCeilingTs, userConfig, viewState
} from '../core/state.ts';
import { Formatter, TimeManager, getISOWeek, getWeekKey } from '../domain/time.ts';

async function exportData() { let s; try { s = await app.DBManager.getStorage(); if (!s) { bbglError("Export Error: Local database is inaccessible or empty. Cannot export data.\n\nRecommendation: Please refresh the page and try again. If you are using Private Browsing or have strict storage limits enabled, you may need to disable them for Torn.com to allow the Gym Log to save and export data."); return; } } catch (e) { bbglError("Export Error: " + (e.message || "Failed to read local database.") + "\n\nRecommendation: Please refresh the page. Ensure your browser is not blocking local storage for Torn.com."); return; } const active = app.getActiveHistory(); let activeCount = 0; [...(active.history || []), active.today].filter(Boolean).forEach(d => { if (d.series) activeCount += d.series.length; }); if (s.series && s.series.length < activeCount) { if (!confirm(`⚠️ EXPORT WARNING ⚠️\n\nThe exported file will be missing some recent logs visible on your screen due to a database error.\n\nRecommendation: Cancel this export and refresh the browser to reset the connection, then try again.\n\nDownload incomplete file anyway?`)) { return; } } const now = new Date(); const month = CONSTANTS.MONTHS[TimeManager.month(now)]; const day = TimeManager.date(now); const year = TimeManager.year(now); const filename = `BBGymLogData - ${month} ${day}_${year}.json`; app.DataController.buildProgressionCache(); if (historyCache && historyCache.meta && historyCache.meta.stickers) { if (!s.meta) s.meta = {}; s.meta.stickers = historyCache.meta.stickers; } const use24h = !new Intl.DateTimeFormat(navigator.language, { hour: 'numeric' }).format(new Date(0)).match(/AM|PM/i); const ordinal = n => { const sfx = ['th', 'st', 'nd', 'rd'], v = n % 100; return n + (sfx[(v - 20) % 10] || sfx[v] || sfx[0]); }; const fmtReadable = d => `${CONSTANTS.MONTHS[d.getUTCMonth()]} ${ordinal(d.getUTCDate())}, ${d.getUTCFullYear()} - ${String(d.getUTCHours()).padStart(2, '0')}:${String(d.getUTCMinutes()).padStart(2, '0')} UTC`; const tzName = (() => { try { return new Intl.DateTimeFormat('en', { timeZoneName: 'short' }).formatToParts(now).find(p => p.type === 'timeZoneName').value; } catch (e) { return ''; } })(); const fmtTs = ts => { const d = new Date(ts * 1000); const utcStr = `${String(d.getUTCHours()).padStart(2, '0')}:${String(d.getUTCMinutes()).padStart(2, '0')}:${String(d.getUTCSeconds()).padStart(2, '0')} UTC`; const lH = d.getHours(), lM = String(d.getMinutes()).padStart(2, '0'), lS = String(d.getSeconds()).padStart(2, '0'); const localStr = use24h ? `${String(lH).padStart(2, '0')}:${lM}:${lS}` : `${lH % 12 || 12}:${lM}:${lS}${lH >= 12 ? 'pm' : 'am'}`; return `${utcStr} / ${localStr}${tzName ? ` ${tzName}` : ''}`; }; const exportStorage = JSON.parse(JSON.stringify(s)); const itemTotals = {}; Object.keys(ITEM_LOG_META).forEach(id => { const m = ITEM_LOG_META[id]; const g = ITEM_GROUP_LABELS[m.group] || 'Other Items'; if (!itemTotals[g]) itemTotals[g] = {}; itemTotals[g][m.label] = 0; }); (exportStorage.series || []).forEach(e => { if (typeof e.gain === 'number') e.gain = r2(e.gain); if (typeof e.after === 'number') e.after = r2(e.after); if (e.type === 'item' && ITEM_LOG_META[e.logId]) { const m = ITEM_LOG_META[e.logId]; itemTotals[ITEM_GROUP_LABELS[m.group] || 'Other Items'][m.label]++; } }); const getUtcDay = ts => { const d = new Date(ts * 1000); return { label: `${CONSTANTS.MONTHS[d.getUTCMonth()]} ${ordinal(d.getUTCDate())}, ${d.getUTCFullYear()}`, key: `${d.getUTCFullYear()}-${d.getUTCMonth()}-${d.getUTCDate()}` }; }; const getLocalDay = ts => { const d = new Date(ts * 1000); return { label: `${CONSTANTS.MONTHS[d.getMonth()]} ${ordinal(d.getDate())}, ${d.getFullYear()}`, key: `${d.getFullYear()}-${d.getMonth()}-${d.getDate()}` }; }; const log = []; let curDayObj = null, prevLocalKey = null; [...(exportStorage.series || [])].reverse().forEach(e => { const utc = getUtcDay(e.ts), local = getLocalDay(e.ts); if (!curDayObj || utc.key !== curDayObj._k) { curDayObj = { day: `${utc.label} - UTC`, _k: utc.key, _lk: new Set(), entries: [] }; log.push(curDayObj); prevLocalKey = null; } if (prevLocalKey !== null && local.key !== prevLocalKey) curDayObj.entries.push(`── ${local.label} (${tzName}) ──`);else if (prevLocalKey === null && utc.key !== local.key) curDayObj.entries.push(`── ${local.label} (${tzName}) ──`); if (e.type === 'item') { const label = ITEM_LOG_META[e.logId] && ITEM_LOG_META[e.logId].label || `Item ${e.logId}`; const entry = { [label]: e.ts }; if (e.energy) entry.e = e.energy; if (e.energyLost != null) entry.eLost = e.energyLost; if (e.happy) entry.happy = e.happy; if (e.statKey) { entry.stat = e.statKey; entry.gain = e.statGain; } curDayObj.entries.push(entry); } else { curDayObj.entries.push({ at: fmtTs(e.ts), ts: e.ts, stat: e.stat, gain: r2(e.gain), cost: e.cost, after: r2(e.after),
    ...(e.rate !== undefined ? { rate: e.rate } : {}) }); } curDayObj._lk.add(local.key); prevLocalKey = local.key; }); log.forEach(d => { if (d._lk && d._lk.size === 1 && [...d._lk][0] === d._k) d.day = d.day.replace(' - UTC', ` - UTC/${tzName}`); delete d._k; delete d._lk; }); exportStorage.series = log; const achievements = app.computeAchievements(app.getActiveHistory()); const cleanCfg = {}; ALLOWED_CONFIG_KEYS.forEach(k => { if (userConfig[k] !== undefined) { cleanCfg[k] = userConfig[k]; } }); const stickers = exportStorage?.meta?.stickers; if (exportStorage.meta) delete exportStorage.meta.stickers; if (exportStorage.meta) delete exportStorage.meta.syncFloor; let rankedWars; try { const warsRaw = localStorage.getItem(KEYS.WARS_DATA); if (warsRaw) { const wars = JSON.parse(warsRaw); const logCutoff = exportStorage.meta && exportStorage.meta.logStartDate ? exportStorage.meta.logStartDate : 0; const factionHistory = app.getFactionHistory(); rankedWars = Object.entries(wars).filter(([, w]) => w.war && w.war.end && w.war.end >= logCutoff && app.wasInFactionDuringWar(factionHistory, w.factionId, w.war.end)).map(([id, w]) => ({ id, start: w.war && w.war.start, end: w.war && w.war.end, winner: w.war && w.war.winner })); } } catch (e) {} let content = JSON.stringify({ meta: { version: SCRIPT_VERSION, exportedAt: fmtReadable(now), itemTotals, ...(rankedWars ? { rankedWars } : {}) }, config: cleanCfg, achievements, storage: exportStorage, _s: stickers ? JSON.stringify(stickers) : undefined }, null, 2); content = content.replace(/\{\n(\s+)"at": ("[^"]*"),\n\s+"ts": (\d+),\n\s+"stat": ("[^"]*"),\n\s+"gain": ([\d.]+),\n\s+"cost": (\d+),\n\s+"after": ([\d.]+)(?:,\n\s+"rate": ([\d.]+))?\n\s+\}/g, (m, sp, la, ts, st, g, co, a, r) => { let o = '{"at": ' + la + ', "ts": ' + ts + ',\n' + sp + '"stat": ' + st + ',\n' + sp + '"gain": ' + g + ', "cost": ' + co + ',\n' + sp + '"after": ' + a; if (r) o += ', "rate": ' + r; return o + '}'; }); const itemLineLabels = Object.values(ITEM_LOG_META).map(m => m.label.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')).join('|'); content = content.replace(new RegExp('\\{\\n\\s+"(' + itemLineLabels + ')":[^}]*\\}', 'g'), m => m.replace(/\s*\n\s*/g, ' ')); content = content.replace(/\},(\ *\n\ +)\{"at":/g, '},\n$1{"at":'); content = content.replace(/\},(\ *\n([ ]+))"\u2500/g, '},\n\n$2"\u2500'); content = content.replace(/\u2500",(\ *\n([ ]+))\{"at":/g, '\u2500",\n\n$2{"at":'); content = content.replace(/"meta": \{\n\s+"version": "([^"]+)",\n\s+"exportedAt": "([^"]+)"\n\s+\}/, '"meta": {"version": "$1", "exportedAt": "$2"}'); content = content.replace(/"config": \{([\s\S]*?)\n\s+\}(?=,\n\s+"achievements")/, (m, inner) => '"config": {' + inner.replace(/\n\s+/g, ' ').trimStart() + '}'); try { const f = new File([content], filename, { type: 'text/plain' }); if (navigator.canShare && navigator.canShare({ files: [f] }) && window.innerWidth <= 800) { await navigator.share({ title: filename, files: [f] }); return; } } catch (e) {} const blob = new Blob([content], { type: 'application/json' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.style.display = 'none'; a.href = url; a.download = filename; document.body.appendChild(a); a.click(); setTimeout(() => { document.body.removeChild(a); URL.revokeObjectURL(url); }, 3000); }

function importData(f, onDone, opts = {}) { if (!f) { if (onDone) onDone(false); return; } const silent = !!opts.silent; const r = new FileReader(); r.onload = async e => { let ok = false; try { const j = JSON.parse(e.target.result); const val = app.validateImportSchema(j); if (!val.ok) { if (!silent) bbglError(`Import Failed: ${val.msg}`); if (onDone) onDone(false); return; } if (j.storage) { j.storage = app.sanitizeStorageRecord(j.storage); if (j.storage.series && j.storage.series.length && j.storage.series[0] && j.storage.series[0].day) { const labelToItem = {}; Object.keys(ITEM_LOG_META).forEach(id => { labelToItem[ITEM_LOG_META[id].label] = { logId: Number(id), energy: !!ITEM_LOG_META[id].energy }; }); j.storage.series = j.storage.series.flatMap(d => (d.entries || []).filter(e => typeof e === 'object' && e !== null)).reverse(); j.storage.series = j.storage.series.map(e => { if (e && e.ts === undefined && e.stat === undefined) { const k = Object.keys(e).find(key => key !== 'e' && labelToItem[key]); if (k) { const m = labelToItem[k]; const out = { type: 'item', logId: m.logId, ts: e[k] }; if (e.e !== undefined) out.energy = e.e; return out; } } delete e.at; delete e.loggedAt; return e; }); } const importedMeta = j.storage.meta || {}; let stickers = importedMeta.stickers || j._s; if (typeof stickers === 'string') { try { stickers = JSON.parse(stickers); } catch (e) {} } if (!stickers) stickers = {}; j.storage.meta.stickers = stickers; await app.DBManager.setStorage(j.storage); const rebuilt = app.DataController._rebuildFromSeries(j.storage.series || [], j.storage.meta && j.storage.meta.baselineBreakdown || ZERO_BREAKDOWN); setHistoryCache({ meta: { ...importedMeta, stickers }, history: rebuilt.history, today: rebuilt.today }); if (j.config && typeof j.config === 'object') { ALLOWED_CONFIG_KEYS.forEach(k => { if (j.config[k] !== undefined) userConfig[k] = j.config[k]; }); if (!userConfig.privacyAgreed || isNaN(Date.parse(userConfig.privacyAgreed))) { userConfig.privacyAgreed = new Date().toISOString(); } saveConfig(); } try { const importedVer = j && j.meta && j.meta.version ? String(j.meta.version) : ''; const curSeen = localStorage.getItem(KEYS.CHANGELOG_VER); if (!curSeen) { if (importedVer) localStorage.setItem(KEYS.CHANGELOG_VER, importedVer);else if (SCRIPT_VERSION) localStorage.setItem(KEYS.CHANGELOG_VER, SCRIPT_VERSION); } const seenNow = localStorage.getItem(KEYS.CHANGELOG_VER); if (SCRIPT_VERSION && seenNow && seenNow !== SCRIPT_VERSION) { localStorage.setItem(KEYS.CHANGELOG_NOTIF, '1'); app.syncChangelogNotif(true); } } catch (e) {} app.DataController.invalidate(); calendarState.selectedData = null; calendarState.selectedLabel = null; viewState.activeViewLabel = null; ok = true; if (!silent) app.renderPanelContent(); if (!silent) alert("Training Data Imported Successfully."); } else if (!silent) bbglError("Error: No valid training data found."); } catch (err) { if (!silent) bbglError("Error importing file: " + (err.message === "Database not initialized" ? "Database not initialized.\n\nRecommendation: Refresh the page and ensure your browser is not blocking local storage for Torn.com." : "Invalid JSON format.")); } const inp = document.getElementById('import-file'); if (inp) inp.value = ''; const inp2 = document.getElementById('init-import-file'); if (inp2) inp2.value = ''; if (onDone) onDone(ok); }; r.readAsText(f); }

function importDataFromWelcome(f) { importData(f, async success => { if (!success) return; app.refreshInitLock(); if (!userConfig.apiKey) { app.renderPanelContent(); const wv = dom.welcomeView; if (wv && wv.classList.contains('active-view')) app.refreshInitMask(wv); return; } try { const res = await fetch(`https://api.torn.com/user/?selections=battlestats,log&log=5300&key=${userConfig.apiKey}`); const data = await res.json(); if (data.error) { bbglError(`Saved API key is no longer valid: ${tornKeyErrorText(data)}\n\nPlease enter a new key to continue.`); userConfig.apiKey = ''; saveConfig(); app.refreshInitLock(); app.renderPanelContent(); const wv = dom.welcomeView; if (wv && wv.classList.contains('active-view')) { app.refreshInitMask(wv); const iak = wv.querySelector('#init-api-key'); if (iak) iak.value = ''; } return; } localStorage.setItem('bbgl_initialized', '1'); app.refreshInitLock(); calendarState.selectedData = null; calendarState.selectedLabel = Formatter.dateLogical(); viewState.activeViewLabel = null; app.switchView('ledger'); app.syncWithFeedback('FULL_SYNC'); } catch (e) { bbglError(MSG_KEY_NETWORK_ERROR); app.renderPanelContent(); const wv = dom.welcomeView; if (wv && wv.classList.contains('active-view')) app.refreshInitMask(wv); } }, { silent: true }); }

async function clearData() { if (confirm("⚠️ CLEAR LOG HISTORY? ⚠️\n\nThis will permanently delete your training data.\n\nUse 'Export Log' before proceeding to preserve it.")) { await app.DBManager.clearStorage(); const keep = [KEYS.CONFIG, KEYS.STATE, 'bbgl_initialized']; for (let i = localStorage.length - 1; i >= 0; i--) { const k = localStorage.key(i); if (k && k.startsWith('bbgl_') && k !== KEYS.STORAGE && !keep.includes(k)) localStorage.removeItem(k); } sessionStorage.removeItem(KEYS.SESSION); sessionStorage.removeItem(KEYS.SESSION_CACHE); app.DataController.invalidate(); setHistoryCache(null); calendarState.selectedData = null; calendarState.selectedLabel = null; viewState.activeViewLabel = null; runtime.apiCallTotal = 0; runtime.stickerSlots = []; runtime.careerLevelExp = 0; runtime._lastLevelExp = undefined; runtime._targetLevelExp = undefined; runtime._isAnimatingLevel = false; app.renderPanelContent(); alert("History cleared."); } }

async function factoryReset() { await app.DBManager.clearStorage(); for (let i = localStorage.length - 1; i >= 0; i--) { const k = localStorage.key(i); if (k && k.startsWith('bbgl_')) localStorage.removeItem(k); } sessionStorage.removeItem(KEYS.SESSION); sessionStorage.removeItem(KEYS.SESSION_CACHE); app.DataController.invalidate(); setHistoryCache(null); calendarState.selectedData = null; calendarState.selectedLabel = null; viewState.activeViewLabel = null; runtime.apiCallTotal = 0; runtime.stickerSlots = []; runtime.currentStats = null; runtime._achPage = 0; const _fresh = { apiKey: '', dayStartMode: 'utc', weekStartMode: 'mon', animations: true, buttonLocation: 'both', ratesEnabled: true, bestGym: true, bestGymSpecialist: true, bestGymUnpurchased: true, drugTracker: 'xanax', privacyAgreed: '' }; ALLOWED_CONFIG_KEYS.forEach(k => { userConfig[k] = _fresh[k] !== undefined ? _fresh[k] : userConfig[k]; }); saveConfig(); localStorage.setItem(KEYS.CHANGELOG_NOTIF, '1'); runtime.wasVersionWiped = true; }

app.exportData = exportData;
app.importData = importData;
app.importDataFromWelcome = importDataFromWelcome;
app.clearData = clearData;
app.factoryReset = factoryReset;
export { exportData, importData, importDataFromWelcome, clearData, factoryReset };
