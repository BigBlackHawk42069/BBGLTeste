import { app } from '../app-context.js';
import {
  ACH_FMT, BACKFILL, BACKFILL_GROUP_KEYS, BACKFILL_GROUP_OF, BACKFILL_GROUPS,
  BASE_DOCS_URL, BBGL_ERROR_CODE, BS_STAT_ROWS, compareVersions, CONSTANTS, ECAN_LOG, ECSTASY_LOG, ENERGY_LOGS, ENERGY_PARAM,
  EX_OD_LOG, GAME, GYM_TIERS, HAPPY_LOGS, ITEM_GROUP_LABELS, ITEM_LOG_META, ITEM_LOGS, KEYS, LAYOUT, LSD_OD_LOG,
  MSG_CLIPBOARD_DENIED, MSG_KEY_FORMAT_INVALID, MSG_KEY_NETWORK_ERROR, MSG_SYNC_NETWORK_ERROR, MSG_SYNC_QUOTA,
  OD_LOGS, r2, SCRIPT_VERSION, STAT_ENHANCER_PARAM, STAT_HAPPY_PARAM, STAT_KEYS, STAT_LOGS, SYNC_FROM_BUFFER,
  TORN_KEY_ERROR_MAP, TRAIN_ENERGY_PARAM, TRAIN_LOGS, WIPE_BELOW_VERSION, XANAX_LOG, XANAX_OD_LOG, ZERO_BREAKDOWN,
  bbglError, tornKeyErrorText
} from '../core/constants.ts';
import { Log, Perf } from '../core/log.ts';
import {
  ALLOWED_CONFIG_KEYS, TAB_ID, calendarState, dom, graphState, historyCache, lastButtonLocation, layoutObservers,
  refreshClickLog, runtime, saveConfig, saveViewState, setHistoryCache, setLastButtonLocation, setTopCeiling, setViewState,
  topCeilingCache, topCeilingTs, userConfig, viewState
} from '../core/state.ts';
import { Formatter, TimeManager, getISOWeek, getWeekKey } from '../domain/time.ts';

function setupEventListeners(root) { app.cacheDOM(root); const get = id => root.querySelector('#' + id); const atBtn = get('all-time-btn'); if (atBtn) atBtn.onclick = e => { e.stopPropagation(); app.calcAllTimeStats(); }; const csb = root.querySelector('#bbgl-settings-view .close-settings-btn'); if (csb) csb.onclick = app.toggleSettingsView; if (dom.ledgerView) { dom.ledgerView.addEventListener('click', e => { const label = e.target.closest('.bbgl-copy-label'); if (!label) return; const col = label.closest('.stat-column'); if (!col) return; const k = col.getAttribute('data-copy-stat'); if (!k) return; const cs = runtime.currentStats; if (!cs) return; const { sl, s } = cs; if (!s[k]) return; const txt = app.buildSessionText(sl, s, [k]); navigator.clipboard.writeText(txt).then(() => app.flashCopied(col)); }); } const sp = get('sticker-prev-btn'), sn = get('sticker-next-btn'),
    ssp = get('sticker-sponsor-btn'); if (sp) sp.onclick = e => { e.stopPropagation(); if (runtime.currentStickerPage > 0) app.changeStickerPage(-1); }; if (sn) sn.onclick = e => { e.stopPropagation(); if (runtime.currentStickerPage < Math.ceil((runtime.stickerData.length || 0) / 10) - 1) app.changeStickerPage(1); }; if (ssp) ssp.onclick = e => { e.stopPropagation(); if (ssp.classList.contains('disabled')) return; if (runtime.currentStickerPage === 0) app.changeStickerPage(-1); }; const pm = get('prev-month-btn'); if (pm) pm.onclick = () => app.changeMonth(-1); const nm = get('next-month-btn'); if (nm) nm.onclick = () => app.changeMonth(1); const mt = get('month-trigger'); if (mt) mt.onclick = e => { e.stopPropagation(); app.toggleMonthDropdown(); }; const yt = get('year-trigger'); if (yt) yt.onclick = e => { e.stopPropagation(); app.toggleYearDropdown(); }; const ms = get('month-stats-btn'); if (ms) ms.onclick = e => { e.stopPropagation(); app.calcPeriodStats('month'); }; const ys = get('year-stats-btn'); if (ys) ys.onclick = e => { e.stopPropagation(); app.calcPeriodStats('year'); }; const at = get('set-anim-toggle'); if (at) { at.checked = userConfig.animations; at.onchange = () => { userConfig.animations = at.checked; saveConfig(); if (dom.panel) dom.panel.classList.toggle('bbgl-no-animations', !userConfig.animations); app.renderPanelContent(); }; } const rt = get('set-rate-toggle'); if (rt) { rt.checked = userConfig.ratesEnabled; rt.onchange = () => { userConfig.ratesEnabled = rt.checked; saveConfig(); if (dom.panel) dom.panel.classList.toggle('bbgl-no-rates', !userConfig.ratesEnabled); if (!userConfig.ratesEnabled && graphState.mode === 'rates') { graphState.mode = 'values'; viewState.graphMode = 'values'; saveViewState(); } const tp = dom.topPanel; if (tp && tp.classList.contains('viewing-graph')) { app.GraphController.restoreUi(); app.GraphController.draw(); } else { const sd = calendarState.selectedData; app.renderStats(sd || app.getActiveHistory().today, calendarState.selectedLabel || Formatter.dateLogical()); } }; } const dtk = get('set-drug-tracker'); if (dtk) { dtk.value = userConfig.drugTracker || 'xanax'; dtk.onchange = () => { userConfig.drugTracker = dtk.value; saveConfig(); const tp = dom.topPanel; if (!tp || !tp.classList.contains('viewing-graph')) { const sd = calendarState.selectedData; app.renderStats(sd || app.getActiveHistory().today, calendarState.selectedLabel || Formatter.dateLogical()); } }; } const agt = get('set-bestgym-toggle'); if (agt) { agt.checked = userConfig.bestGym; agt.onchange = () => app.setBestGym(agt.checked); } const ags = get('set-bestgym-spec-toggle'); if (ags) { ags.checked = userConfig.bestGymSpecialist; const agsRow = ags.closest('.bbgl-setting-row'); if (agsRow) agsRow.classList.toggle('bbgl-row-disabled', !userConfig.bestGym); ags.onchange = () => { userConfig.bestGymSpecialist = ags.checked; saveConfig(); }; } const agu = get('set-bestgym-unpurch-toggle'); if (agu) { agu.checked = userConfig.bestGymUnpurchased; const aguRow = agu.closest('.bbgl-setting-row'); if (aguRow) aguRow.classList.toggle('bbgl-row-disabled', !userConfig.bestGym); agu.onchange = () => { userConfig.bestGymUnpurchased = agu.checked; saveConfig(); }; } const ls = get('set-loc-select'); if (ls) { ls.value = userConfig.buttonLocation; ls.onchange = () => app.onChangeLoc(ls.value); } const ds = get('set-day-start'); if (ds) { ds.value = userConfig.dayStartMode; ds.onchange = () => app.onChangeDayStart(ds.value); } const ws = get('set-week-start'); if (ws) { ws.value = userConfig.weekStartMode; ws.onchange = () => app.onChangeWeekStart(ws.value); } const ai = get('set-api-key'), ap = get('set-api-paste'); if (ap && ai) ap.onclick = async () => { try { const t = await navigator.clipboard.readText(); if (t) ai.value = t.trim(); } catch (e) { bbglError(MSG_CLIPBOARD_DENIED); } }; const ub = get('updt-settings-btn'); if (ub && ai) ub.onclick = async function () { this.blur(); const v = ai.value.trim(); if (!/^[a-zA-Z0-9]{16}$/.test(v)) { bbglError(MSG_KEY_FORMAT_INVALID); return; } const ot = ub.innerText; ub.innerText = "VERIFYING..."; try { const res = await fetch(`https://api.torn.com/user/?selections=battlestats,log&log=5300&key=${v}`), data = await res.json(); if (data.error) { bbglError(`Key Verification Failed: ${tornKeyErrorText(data)}`); ub.innerText = ot; return; } userConfig.apiKey = v; saveConfig(); ub.style.transition = "all 0.2s"; ub.style.color = "#69f0ae"; ub.style.borderColor = "#69f0ae"; ub.innerText = "KEY SAVED"; if (ub.dataset.timer) clearTimeout(ub.dataset.timer); ub.dataset.timer = setTimeout(() => { ub.style.color = ""; ub.style.borderColor = ""; ub.innerText = ot; },
    2000); } catch (e) { bbglError(MSG_KEY_NETWORK_ERROR); ub.innerText = ot; } }; const cab = get('clear-api-btn'); if (cab && ai) cab.onclick = function () { this.blur(); userConfig.apiKey = ''; saveConfig(); ai.value = ''; localStorage.removeItem(KEYS.LAST_SYNC); sessionStorage.removeItem(KEYS.SESSION_CACHE); sessionStorage.removeItem(KEYS.SESSION); const ot = cab.innerText; cab.innerText = "WIPED"; setTimeout(() => { cab.innerText = ot; }, 2000); }; const crb = get('create-api-btn'); if (crb) crb.onclick = function () { this.blur(); window.open('https://www.torn.com/preferences.php#tab=api?step=addNewKey&user=basic,battlestats,log&faction=rankedwars&logIds=54,50,23,6,52,56,3&title=BigBlackGymLog', '_blank'); }; const rb = get('refresh-log-btn'); if (rb) rb.onclick = function () { this.blur(); if (app.checkRefreshCooldown(this)) return; app.syncWithFeedback('FULL_SYNC'); }; const rsb = get('resync-btn'); if (rsb) rsb.onclick = async function () { this.blur(); app.setResyncBtnState(rsb, 'syncing'); await app.syncWithFeedback('FULL_SYNC'); app.setResyncBtnState(rsb, 'done'); if (rsb.dataset.timerId) clearTimeout(rsb.dataset.timerId); rsb.dataset.timerId = setTimeout(() => app.setResyncBtnState(rsb, 'idle'), 2000); }; const eb = get('export-btn'); if (eb) eb.onclick = function () { this.blur(); app.exportData(); }; const ib = get('import-btn'); if (ib) ib.onclick = function () { this.blur(); get('import-file').click(); }; const iF = get('import-file'); if (iF) iF.onchange = e => app.importData(e.target.files[0]); app.renderBackfillButton(); app.renderScanOverlay(); const clb = get('clear-btn'); if (clb) clb.onclick = function () { this.blur(); app.clearData(); }; const pdb = get('settings-privacy-btn'); if (pdb) pdb.onclick = function () { this.blur(); app.openPrivacyModal(); }; const cl = get('settings-changelog-btn'); if (cl) cl.onclick = function () { this.blur(); app.openChangelogModal(); }; const sdemo = get('settings-demo-btn'); if (sdemo) sdemo.onclick = function () { this.blur(); if (runtime.demoMode) { const deb = document.getElementById('bbgl-demo-exit'); if (deb) deb.click(); } else { app.enterDemoFromSettings(); } }; const fgb = get('feature-guide-btn'); if (fgb) fgb.onclick = function () { this.blur(); app.openFeatureGuideModal(); }; const sa = get('swipe-area'); if (sa) { let _sX = 0, _sY = 0; sa.addEventListener('touchstart', e => { _sX = e.touches[0].clientX; _sY = e.touches[0].clientY; }, { passive: true }); sa.addEventListener('touchend', e => { if (window._bbglScrubbing) return; const dx = e.changedTouches[0].clientX - _sX, dy = e.changedTouches[0].clientY - _sY; if (Math.abs(dx) > 50 && Math.abs(dx) > Math.abs(dy) * 1.5) app.changeMonth(dx < 0 ? 1 : -1); }, { passive: true }); } const sgSwipe = get('bbgl-sticker-container'); if (sgSwipe) { let _sgX = 0, _sgY = 0; sgSwipe.addEventListener('touchstart', e => { _sgX = e.touches[0].clientX; _sgY = e.touches[0].clientY; }, { passive: true }); sgSwipe.addEventListener('touchend', e => { if (window._bbglScrubbing) return; const dx = e.changedTouches[0].clientX - _sgX, dy = e.changedTouches[0].clientY - _sgY; if (Math.abs(dx) > 40 && Math.abs(dx) > Math.abs(dy) * 1.5) { const dir = dx < 0 ? 1 : -1, maxP = Math.ceil((runtime.stickerData.length || 0) / 10) - 1; if (dir < 0 && runtime.currentStickerPage > -1 || dir > 0 && runtime.currentStickerPage < maxP) app.changeStickerPage(dir); } }, { passive: true }); } app.GraphController.setupControls(); app.setupStickerGrid(); app.refreshInitLock(); const achPrev = get('bbgl-achievements-container') ? root.querySelector('.bbgl-ach-prev') : null; const achNext = get('bbgl-achievements-container') ? root.querySelector('.bbgl-ach-next') : null; if (achPrev) achPrev.onclick = e => { e.stopPropagation(); app.gotoAchievementsPage(-1); }; if (achNext) achNext.onclick = e => { e.stopPropagation(); app.gotoAchievementsPage(1); }; const achContainer = get('bbgl-achievements-container'); if (achContainer) { let _achX = 0, _achY = 0; achContainer.addEventListener('touchstart', e => { _achX = e.touches[0].clientX; _achY = e.touches[0].clientY; }, { passive: true }); achContainer.addEventListener('touchend', e => { if (window._bbglScrubbing) return; const dx = e.changedTouches[0].clientX - _achX, dy = e.changedTouches[0].clientY - _achY; if (Math.abs(dx) > 40 && Math.abs(dx) > Math.abs(dy) * 1.5) { app.gotoAchievementsPage(dx < 0 ? 1 : -1); } }, { passive: true }); achContainer.addEventListener('click', e => { const swOpt = e.target.closest('.bbgl-enh-sw-opt'); if (swOpt) { const toSelected = swOpt.dataset.mode === 'selected'; if (toSelected !== !!viewState.achEnhPeriodMode) { viewState.achEnhPeriodMode = toSelected; saveViewState(); app.achRefreshPageDom(); } return; } const colHeader = e.target.closest('.bbgl-ach-col-copy'); if (colHeader) { app.handleAchCopy(colHeader); return; } const statCell = e.target.closest('.bbgl-ach-stat-cell'); if (statCell) { app.handleAchCopy(statCell); return; } const group = e.target.closest('.bbgl-ach-hh-group'); if (group) { app.handleAchCopy(group); return; } const row = e.target.closest('.bbgl-ach-section-title, .bbgl-ach-subsection-title, .bbgl-ach-row'); if (row) app.handleAchCopy(row); }); } }

app.setupEventListeners = setupEventListeners;
export { setupEventListeners };
