import { app } from '../app-context.js';
import { ICONS } from '../ui/icons.ts';
import {
  ACH_FMT, BACKFILL, BACKFILL_GROUP_KEYS, BACKFILL_GROUP_OF, BACKFILL_GROUPS,
  BASE_DOCS_URL, BBGL_ERROR_CODE, BS_STAT_ROWS, compareVersions, CONSTANTS, ECAN_LOG, ECSTASY_LOG, ENERGY_LOGS, ENERGY_PARAM,
  EX_OD_LOG, GAME, GYM_TIERS, HAPPY_LOGS, ITEM_GROUP_LABELS, ITEM_LOG_META, ITEM_LOGS, KEYS, LAYOUT, LSD_OD_LOG,
  MSG_CLIPBOARD_DENIED, MSG_KEY_FORMAT_INVALID, MSG_KEY_NETWORK_ERROR, MSG_SYNC_NETWORK_ERROR, MSG_SYNC_QUOTA,
  OD_LOGS, r2, SCRIPT_VERSION, STAT_ENHANCER_PARAM, STAT_HAPPY_PARAM, STAT_KEYS, STAT_LOGS, SYNC_FROM_BUFFER,
  TORN_KEY_ERROR_MAP, TRAIN_ENERGY_PARAM, TRAIN_LOGS, WIPE_BELOW_VERSION, XANAX_LOG, XANAX_OD_LOG, ZERO_BREAKDOWN,
  bbglError, tornKeyErrorText
} from '../core/constants.ts';
import { Log, Perf } from '../core/log.ts';
import {
  ALLOWED_CONFIG_KEYS, TAB_ID, calendarState, dom, graphState, historyCache, lastButtonLocation, layoutObservers,
  refreshClickLog, runtime, saveConfig, saveViewState, setHistoryCache, setLastButtonLocation, setTopCeiling, setViewState,
  topCeilingCache, topCeilingTs, userConfig, viewState
} from '../core/state.ts';
import { Formatter, TimeManager, getISOWeek, getWeekKey } from '../domain/time.ts';
import { computeWeekCompletion } from '../domain/capsules.ts';
import { atrophyTitle, calculateLevelProgress, computeDailyLevelExp } from '../domain/leveling.ts';
import { findHappyJumps } from '../domain/day.ts';


function injectWeeklyBar(cont, batch) { const sl = app.DataController.getSlice('CUSTOM', batch.map(w => w.data).filter(d => d)); sl.label = `Week ${getISOWeek(batch[0].date)}`; sl._weekStart = batch[0].date; sl._weekEnd = batch[batch.length - 1].date; if (sl._dailyList.length === 0) return; const { hjDaySet } = app.DataController.getHappyJumpData(); const _wk = getWeekKey(sl._dailyList[0].date); const anchor = document.createElement('div'); anchor.className = 'bbgl-weekly-anchor'; const tr = document.createElement('div'); tr.className = 'bbgl-weekly-track'; tr.dataset.label = sl.label; tr.onclick = e => { e.stopPropagation(); app.openHistory(sl, sl.label); }; if (calendarState.selectedLabel === sl.label) tr.classList.add('is-viewing'); const installWeekKey = runtime.demoMode ? null : app.getInstallWeekKey(); const addCenterTab = slice => { const tab = document.createElement('div'); tab.className = 'bbgl-bar-handle'; tab.dataset.pos = 'start'; const tooltipHtml = app.generateRichTooltip(slice); tab.setAttribute('data-tooltip-html', tooltipHtml); tab.setAttribute('data-tooltip-anchor', '.bbgl-bar-handle'); tr.setAttribute('data-tooltip-html', tooltipHtml); tr.setAttribute('data-tooltip-anchor', '.bbgl-bar-handle'); tab.onclick = e => { e.stopPropagation(); app.openHistory(slice, slice.label); }; tab.addEventListener('mouseenter', () => tr.classList.add('is-scrub-hovered')); tab.addEventListener('mouseleave', () => tr.classList.remove('is-scrub-hovered')); tab.innerHTML = app.buildChartSVG(slice); anchor.appendChild(tab); }; if (installWeekKey && _wk < installWeekKey) { tr.innerHTML = app.buildCapsuleBar(['silver', 'silver', 'silver', 'silver', 'silver'], false, false); anchor.appendChild(tr); addCenterTab(sl); cont.appendChild(anchor); if (viewState.activeViewLabel === sl.label && calendarState.selectedLabel !== sl.label) runtime._pendingHistoryRestore = { sl, label: sl.label }; return; } const { capsules, isCompleted } = computeWeekCompletion(sl._dailyList, hjDaySet); if (isCompleted) tr.classList.add('track-polished'); tr.innerHTML = app.buildCapsuleBar(capsules, isCompleted, isCompleted && userConfig.animations); anchor.appendChild(tr); addCenterTab(sl); cont.appendChild(anchor); if (viewState.activeViewLabel === sl.label && calendarState.selectedLabel !== sl.label) runtime._pendingHistoryRestore = { sl, label: sl.label }; }

function getLiveLevelExp() { let totalExp = app.DataController.getCareerLevelExp(); if (!runtime.demoMode) { const h = app.getActiveHistory(); if (h && h.today) { const today = Formatter.dateLogical(); const installDateKey = app.getInstallDateKey(); const rewardStartTs = h.meta && h.meta.rewardStartDate || null; let todaySeries = h.today.series || []; if (installDateKey && today === installDateKey && rewardStartTs) { todaySeries = todaySeries.filter(s => s.ts >= rewardStartTs); } const todayE = todaySeries === h.today.series && h.today.eSpent ? h.today.eSpent.total || 0 : todaySeries.filter(s => s.type === 'gym').reduce((sum, s) => sum + (s.cost || 0), 0); const hasTrainLog = todaySeries.some(s => s.type === 'gym'); const { hjDaySet } = app.DataController.getHappyJumpData(); const isHJ = todaySeries === h.today.series ? hjDaySet.has(today) : findHappyJumps(todaySeries).length > 0; totalExp += computeDailyLevelExp(todayE, hasTrainLog, isHJ); } } return totalExp; }

function getLevelBars() { return [['bbgl-level-num', 'bbgl-level-fill', 'bbgl-level-container'], ['bbgl-gym-level-num', 'bbgl-gym-level-fill', 'bbgl-gym-level-container']].map(([n, f, c]) => ({ num: document.getElementById(n), fill: document.getElementById(f), container: document.getElementById(c) })).filter(b => b.num && b.fill && b.container); }

function renderLevelBar(bar, expVal) { const { atrophy, level, expInLevel, expToNext } = calculateLevelProgress(expVal); const pct = expToNext > 0 ? Math.min(100, expInLevel / expToNext * 100) : level >= 100 ? 100 : 0; bar.num.innerHTML = '<span class="bbgl-lv-prefix">Lv </span>' + level; bar.fill.style.width = (pct / 100 * 96.8).toFixed(2) + '%'; bar.fill.classList.toggle('level-full', pct >= 99.9); if (dom.panel) { dom.panel.dataset.atrophy = atrophy; dom.panel.dataset.level = level; } bar.container.dataset.atrophy = atrophy; bar.container.dataset.level = level; const lvLine = level >= 100 ? 'Level 100  •  Max Level' : `Level ${level}  •  ${Math.round(pct)}%`; bar.container.setAttribute('data-tooltip', `${lvLine}<br><i class="bbgl-lvl-tip-title">${atrophyTitle(atrophy, level)}</i>`); }

function handleDomMutation() { injectGymLevelBar(); if (!dom.bestGym || !dom.bestGym.isConnected) app.injectBestGymToggle(); const loc = userConfig.buttonLocation, showFooter = loc === 'notes' || loc === 'both', showSidebar = loc === 'sidebar' || loc === 'both'; if (loc !== lastButtonLocation && !runtime._domObsArmed) rearmDomObs(); if (loc === lastButtonLocation) { const gtCached = dom.gymTab && dom.gymTab.isConnected ? dom.gymTab : null; const sbDCached = dom.sbDesktop && dom.sbDesktop.isConnected ? dom.sbDesktop : null; const sbMCached = dom.sbMobile && dom.sbMobile.isConnected ? dom.sbMobile : null; const footerOk = !showFooter || !!gtCached; const sidebarOk = !showSidebar || !!sbDCached; if (footerOk && sidebarOk) { if (!gtCached) dom.gymTab = null; if (!sbDCached) dom.sbDesktop = null; if (!sbMCached) dom.sbMobile = null; if (showFooter && gtCached && dom.notesBtn && dom.notesBtn.isConnected && gtCached.nextSibling !== dom.notesBtn) { dom.notesBtn.parentNode.insertBefore(gtCached, dom.notesBtn); } if (showSidebar) { app.syncSidebarState(); if (!dom.sbMobile || !dom.sbMobile.isConnected) { const mt = document.querySelector(SB_MOBILE.target); if (mt) { injectSidebarButton(SB_MOBILE, true); dom.sbMobile = document.getElementById(SB_MOBILE.id); } } if (!dom.sbFlyout || !dom.sbFlyout.isConnected) { const ft = document.querySelector(SB_FLYOUT.target); if (ft) { injectSidebarButton(SB_FLYOUT, true); dom.sbFlyout = document.getElementById(SB_FLYOUT.id); } } } settleDomObs(); return; } } Perf.start('handleDomMutation'); const _prevNotesBtn = dom.notesBtn, _prevPeopBtn = dom.peopleBtn, _prevSettBtn = dom.settingsBtn, _prevChatRoot = dom.chatRoot; if (!dom.notesBtn || !dom.notesBtn.isConnected) dom.notesBtn = document.getElementById('notes_panel_button'); if (!dom.peopleBtn || !dom.peopleBtn.isConnected) dom.peopleBtn = document.getElementById('people_panel_button'); if (!dom.settingsBtn || !dom.settingsBtn.isConnected) dom.settingsBtn = document.getElementById('notes_settings_button'); if (!dom.chatRoot || !dom.chatRoot.isConnected) dom.chatRoot = _bbglGetChatRoot(); if (dom.notesBtn !== _prevNotesBtn || dom.peopleBtn !== _prevPeopBtn || dom.settingsBtn !== _prevSettBtn || dom.chatRoot !== _prevChatRoot) attachLayoutObservers(); if (!dom.gymTab || !dom.gymTab.isConnected) dom.gymTab = document.getElementById('bbgl-gym-tab'); const mb = dom.gymTab; if (!dom.sbDesktop || !dom.sbDesktop.isConnected) dom.sbDesktop = document.getElementById(SB_DESKTOP.id); if (!dom.sbMobile || !dom.sbMobile.isConnected) dom.sbMobile = document.getElementById(SB_MOBILE.id); if (!dom.sbFlyout || !dom.sbFlyout.isConnected) dom.sbFlyout = document.getElementById(SB_FLYOUT.id); if (showSidebar && (!dom.sbDesktop || !dom.sbMobile)) { if (!dom.sbDesktopTarget || !dom.sbDesktopTarget.isConnected) dom.sbDesktopTarget = document.querySelector(SB_DESKTOP.target); if (!dom.sbMobileTarget || !dom.sbMobileTarget.isConnected) dom.sbMobileTarget = document.querySelector(SB_MOBILE.target); } if (showSidebar && !dom.sbFlyout) { if (!dom.sbFlyoutTarget || !dom.sbFlyoutTarget.isConnected) dom.sbFlyoutTarget = document.querySelector(SB_FLYOUT.target); } const nb = dom.notesBtn; if (showFooter && nb && !mb) { injectFooterButton(nb); dom.gymTab = document.getElementById('bbgl-gym-tab'); } else if (showFooter && nb && mb && mb.nextSibling !== nb) { nb.parentNode.insertBefore(mb, nb); } else if (!showFooter && mb) { mb.remove(); dom.gymTab = null; } const dt = dom.sbDesktopTarget, mt = dom.sbMobileTarget, ft = dom.sbFlyoutTarget; if (showSidebar) { if (dt && !dom.sbDesktop) { injectSidebarButton(SB_DESKTOP, false); dom.sbDesktop = document.getElementById(SB_DESKTOP.id); } if (mt && !dom.sbMobile) { injectSidebarButton(SB_MOBILE, true); dom.sbMobile = document.getElementById(SB_MOBILE.id); } if (ft && !dom.sbFlyout) { injectSidebarButton(SB_FLYOUT, true); dom.sbFlyout = document.getElementById(SB_FLYOUT.id); } } else { const dEl = dom.sbDesktop, mEl = dom.sbMobile, fEl = dom.sbFlyout; if (dEl) { const sl = dEl.closest('.swiper-slide'); sl ? sl.remove() : dEl.remove(); dom.sbDesktop = null; } if (mEl) { const sl = mEl.closest('.swiper-slide'); sl ? sl.remove() : mEl.remove(); dom.sbMobile = null; } if (fEl) { fEl.remove(); dom.sbFlyout = null; } } setLastButtonLocation(loc); if (showSidebar) app.syncSidebarState(); settleDomObs(); Perf.end('handleDomMutation'); }

function settleDomObs() { const loc = userConfig.buttonLocation, showFooter = loc === 'notes' || loc === 'both', showSb = loc === 'sidebar' || loc === 'both'; const footerOk = !showFooter || dom.gymTab && dom.gymTab.isConnected; const sidebarOk = !showSb || dom.sbDesktop && dom.sbDesktop.isConnected; if (!footerOk || !sidebarOk) return; if (!runtime.domObs || !runtime._domObsArmed) return; runtime.domObs.disconnect(); runtime._domObsArmed = false; const guard = parent => { if (!parent) return; const o = new MutationObserver(() => { if (!runtime._domObsArmed) rearmDomObs(); }); o.observe(parent, { childList: true }); runtime._domGuards.push(o); }; const seen = new Set(); [dom.gymTab && dom.gymTab.parentNode, dom.sbDesktop && dom.sbDesktop.parentNode, dom.sbMobile && dom.sbMobile.parentNode, dom.sbFlyout && dom.sbFlyout.parentNode].forEach(p => { if (p && !seen.has(p)) { seen.add(p); guard(p); } }); }

function rearmDomObs() { if (!runtime.domObs || runtime._domObsArmed) return; runtime._domGuards.forEach(o => o.disconnect()); runtime._domGuards = []; runtime.domObs.observe(document.body, { childList: true, subtree: true }); runtime._domObsArmed = true; if (runtime._domRearmRaf) return; runtime._domRearmRaf = requestAnimationFrame(() => { runtime._domRearmRaf = null; handleDomMutation(); }); }

const SB_DESKTOP = { target: '#nav-gym[class*="area-desktop"]', container: 'area-desktop___vZLI8', link: 'desktopLink___SG2RU', row: 'area-row___iBD8N', id: 'nav-gym-log-desktop' }
, SB_MOBILE = { target: '#nav-gym[class*="area-mobile"]:not(#fly-out-panel *)', container: 'area-mobile___sx8BQ', link: 'mobileLink___xTgRa sidebarMobileLink', row: 'area-row___iBD8N', slide: 'swiper-slide slide___se7hj', id: 'nav-gym-log-mobile' }, SB_FLYOUT = { target: '#fly-out-panel [id="nav-gym"]', container: 'area-mobile___AK1cR notList___jrp60', link: 'link___tg6eQ mobileLink___NbSV4', row: 'areaRow___Eheay', id: 'nav-gym-log-flyout' }, GYM_LOG_ICON = `<svg xmlns="http://www.w3.org/2000/svg" stroke="transparent" stroke-width="0" width="18" height="18" viewBox="60 20 280 215"><g transform="scale(1, 1.15)"><path d="${ICONS.LOGO_PATH}"></path></g></svg>`;

function handleLayout() { const p = dom.panel, tb = dom.gymTab, isPanelOpen = p && p.style.display !== 'none'; if (!p || p.classList.contains('bbgl-mode-page')) { if (tb) tb.classList.toggle('bbgl-tab-active', !!isPanelOpen); return; } const peopBtn = dom.peopleBtn && dom.peopleBtn.isConnected ? dom.peopleBtn : dom.peopleBtn = document.getElementById('people_panel_button'); const settBtn = dom.settingsBtn && dom.settingsBtn.isConnected ? dom.settingsBtn : dom.settingsBtn = document.getElementById('notes_settings_button'); const noteBtn = dom.notesBtn && dom.notesBtn.isConnected ? dom.notesBtn : dom.notesBtn = document.getElementById('notes_panel_button'); const chatRoot = dom.chatRoot && dom.chatRoot.isConnected ? dom.chatRoot : dom.chatRoot = _bbglGetChatRoot(); const isOpen = b => b && b.className.includes('opened___'); const peopOpen = isOpen(peopBtn), settOpen = isOpen(settBtn), notesOpen = isOpen(noteBtn); const innerW = window.innerWidth; const topCeiling = app.getTopCeiling(); const visWins = app._getLayoutWindows(); app._syncLayoutResizeTargets(visWins); let isNotesExpanded = false; let maxNonChatWidth = 0; const winInfo = []; const shoveTargets = _bbglGetChatShoveTargets(); visWins.forEach(w => { const inChat = _bbglIsChatWindow(w, shoveTargets); const rect = w.getBoundingClientRect(); const dist = innerW - rect.right; if (notesOpen && !inChat) maxNonChatWidth = Math.max(maxNonChatWidth, w.offsetWidth || 0); winInfo.push({ w, rect, dist, inChat }); }); if (notesOpen) isNotesExpanded = maxNonChatWidth > 500 || innerW <= 620 && maxNonChatWidth > innerW * 0.75; let off = LAYOUT.BASE_RIGHT; if (peopOpen) off += 303; if (settOpen) off += 303; if (notesOpen) off += isNotesExpanded ? 582 : 303; let pRight, pOpacity, pPointer; if (innerW - off < 40) { pRight = `${innerW + 50}px`; pOpacity = '0'; pPointer = 'none'; } else { const panelWidth = viewState.expanded ? 576 : 300; if (off <= LAYOUT.BASE_RIGHT) { const maxOff = innerW - panelWidth - 0; if (off > maxOff) off = Math.max(0, maxOff); } pRight = `${off}px`; pOpacity = '1'; pPointer = 'auto'; } const totalShift = viewState.expanded ? 581 : 305; if (tb) tb.classList.toggle('bbgl-tab-active', !!isPanelOpen); p.style.setProperty('max-height', `calc(100vh - ${topCeiling}px)`, 'important'); p.style.right = pRight; p.style.opacity = pOpacity; p.style.pointerEvents = pPointer; const _staleParent = shoveTargets[0] && shoveTargets[0].parentElement || null; if (_staleParent && _staleParent.style.transform) _staleParent.style.transform = ''; shoveTargets.forEach(t => { t.style.right = isPanelOpen ? `${totalShift}px` : ''; const _tr = t.style.transition || ''; if (!_tr.includes('right')) t.style.transition = _tr ? _tr + ', right 0.2s ease-out' : 'right 0.2s ease-out'; }); winInfo.forEach(({ w, inChat }) => { if (!inChat) { w.style.transform = ''; return; } }); }

function markPanelResizing(p) { if (!p) return; if (p._bbglResizingCancel) p._bbglResizingCancel(); p.classList.add('bbgl-resizing'); let done = false; const finish = () => { if (done) return; done = true; p.removeEventListener('transitionend', onEnd); clearTimeout(timer); p.classList.remove('bbgl-resizing'); p._bbglResizingCancel = null; }; const onEnd = ev => { if (ev.target === p && (ev.propertyName === 'width' || ev.propertyName === 'height')) finish(); }; p.addEventListener('transitionend', onEnd); const timer = setTimeout(finish, 350); p._bbglResizingCancel = finish; }

function _bbglGetChatRoot() { return document.getElementById('chatRoot'); }

function _bbglGetChatShoveTargets() { const targets = []; document.querySelectorAll('[id^="channel_panel_button:"]').forEach(b => { const id = b.id.slice('channel_panel_button:'.length); if (!id) return; const box = document.getElementById(id); if (!box) return; const wrap = box.closest('[class*="item___"]'); if (wrap && !targets.includes(wrap)) targets.push(wrap); }); return targets; }

function _bbglIsChatWindow(w, shoveTargets) { if (!w) return false; if (shoveTargets && shoveTargets.some(t => t === w || t.contains(w) || w.contains(t))) return true; const cls = w.className || ''; return typeof cls === 'string' && cls.toLowerCase().includes('chat'); }

function attachLayoutObservers() { layoutObservers.forEach(o => { if (o.disconnect) o.disconnect(); }); layoutObservers.length = 0; const onLayoutChange = function onLayoutChange() { if (runtime.layoutRafId) return; runtime.layoutRafId = requestAnimationFrame(function onLayoutFrame() { runtime.layoutRafId = null; app._syncLayoutResizeTargets(); handleLayout(); clearTimeout(runtime._layoutResyncTimer); runtime._layoutResyncTimer = setTimeout(function () { runtime._layoutResyncTimer = null; app._syncLayoutResizeTargets(); }, 350); }); }; if (!runtime.layoutResizeObserver) { runtime.layoutResizeObserver = new ResizeObserver(onLayoutChange); } if (!runtime._layoutWinResizeArmed) { runtime._layoutWinResizeArmed = true; window.addEventListener('resize', onLayoutChange, { passive: true }); } const watchClass = el => { if (!el) return; const o = new MutationObserver(onLayoutChange); o.observe(el, { attributes: true, attributeFilter: ['class'] }); layoutObservers.push(o); }; const watchChatRoot = el => { if (!el) return; const o = new MutationObserver(onLayoutChange); o.observe(el, { childList: true, subtree: true, attributes: true, attributeFilter: ['class'] }); layoutObservers.push(o); }; const watchLayoutLifecycle = () => { const o = new MutationObserver(muts => { for (const m of muts) { if (m.type !== 'childList') continue; const nodes = []; if (m.addedNodes && m.addedNodes.length) nodes.push(...m.addedNodes); if (m.removedNodes && m.removedNodes.length) nodes.push(...m.removedNodes); for (const n of nodes) { const el = n && n.nodeType === 1 ? n : null; if (!el) continue; const cn = el.className || ''; if (typeof cn === 'string' && (cn.includes('visible___') || cn.includes('opened___')) || el.querySelector && el.querySelector('[class*="visible___"], [class*="opened___"]')) { onLayoutChange(); return; } } } }); o.observe(document.body, { childList: true, subtree: true }); layoutObservers.push(o); }; dom.notesBtn = document.getElementById('notes_panel_button'); dom.peopleBtn = document.getElementById('people_panel_button'); dom.settingsBtn = document.getElementById('notes_settings_button'); dom.chatRoot = _bbglGetChatRoot(); watchClass(dom.notesBtn); watchClass(dom.peopleBtn); watchClass(dom.settingsBtn); watchChatRoot(dom.chatRoot); watchLayoutLifecycle(); onLayoutChange(); }

function injectGymLevelBar() { const gymRoot = document.getElementById('gymroot'); if (!gymRoot) return; if (document.getElementById('bbgl-gym-level-container')) return; for (const p of gymRoot.querySelectorAll('p')) { if (p.textContent.trim() === 'What would you like to train today?') { (p.parentElement?.parentElement ?? p).remove(); break; } } const container = document.createElement('div'); container.id = 'bbgl-gym-level-container'; const num = document.createElement('div'); num.id = 'bbgl-gym-level-num'; const track = document.createElement('div'); track.id = 'bbgl-gym-level-track'; const fill = document.createElement('div'); fill.id = 'bbgl-gym-level-fill'; track.innerHTML = app.buildEmptyLevelTrackSVG(); track.appendChild(fill); container.appendChild(num); container.appendChild(track); gymRoot.prepend(container); app.DataController.buildProgressionCache(); renderLevelBar({ num, fill, container }, getLiveLevelExp()); }

function injectFooterButton(notesBtnEl) { if (!notesBtnEl || !notesBtnEl.parentNode) return; if (document.getElementById('bbgl-gym-tab')) return; const b = document.createElement('button'); b.id = 'bbgl-gym-tab'; b.innerHTML = ICONS.LOGO; b.type = 'button'; b.setAttribute('data-tooltip', 'Big Black Gym Log'); notesBtnEl.parentNode.insertBefore(b, notesBtnEl); dom.gymTab = b; app.updateFooterTooltip(); }

function injectSidebarButton(cfg, mob) { if (document.getElementById(cfg.id)) return; const c = document.createElement('div'); c.className = cfg.container; c.id = cfg.id; const r = document.createElement('div'); r.className = cfg.row; const l = document.createElement('a'); l.href = '/calendar.php#gymlog'; l.className = cfg.link; l.innerHTML = `<span class="svgIconWrap___AMIqR"><span class="defaultIcon___iiNis mobile___paLva">${GYM_LOG_ICON}</span></span>${mob ? '<span>Gym Log</span>' : '<span class="linkName___FoKha">Gym Log</span>'}`; const _isNewInstall = !localStorage.getItem('bbgl_initialized') && !localStorage.getItem(KEYS.SB_NOTIF); const _hasChangelogNotif = localStorage.getItem(KEYS.CHANGELOG_NOTIF) === '1'; if (_isNewInstall || _hasChangelogNotif) c.classList.add('bbgl-sb-notif'); l.addEventListener('click', e => { e.preventDefault(); const hadNotif = c.classList.contains('bbgl-sb-notif'); const _liveIsNewInstall = !localStorage.getItem('bbgl_initialized') && !localStorage.getItem(KEYS.SB_NOTIF); const _liveHasChangelogNotif = localStorage.getItem(KEYS.CHANGELOG_NOTIF) === '1'; if (hadNotif) { if (_liveIsNewInstall) localStorage.setItem(KEYS.SB_NOTIF, '1'); if (_liveHasChangelogNotif) app.syncChangelogNotif(false); } if (window.location.pathname !== '/calendar.php' || window.location.hash !== '#gymlog') { window.location.href = '/calendar.php#gymlog'; } else { if (hadNotif && _liveHasChangelogNotif) { localStorage.setItem(KEYS.CHANGELOG_VER, SCRIPT_VERSION); localStorage.removeItem(KEYS.CHANGELOG_NOTIF); setTimeout(() => app.openChangelogModal(), 400); } } }); r.appendChild(l); c.appendChild(r); document.querySelectorAll(cfg.target).forEach(n => { const _liveContainer = Array.from(n.classList).filter(cl => !cl.startsWith('active___')).join(' '); if (_liveContainer) { const hasNotif = c.classList.contains('bbgl-sb-notif'); c.className = _liveContainer; if (hasNotif) c.classList.add('bbgl-sb-notif'); } const _liveRow = n.querySelector('[class*="area-row"], [class*="areaRow"]') || n.firstElementChild; if (_liveRow) r.className = _liveRow.className; const _scopedSiblings = n.parentNode ? Array.from(n.parentNode.children).filter(el => el !== n && el.id && el.id.startsWith('nav-') && el.id !== cfg.id && el.querySelector('a')) : []; const _siblingSelector = mob ? '[id^="nav-"][class*="area-mobile"]' : '[id^="nav-"][class*="area-desktop"]'; const _allSiblings = _scopedSiblings.length ? _scopedSiblings : Array.from(document.querySelectorAll(_siblingSelector)).filter(el => el !== n && el.id !== cfg.id && el.querySelector('a')); const _inactiveSibling = _allSiblings.find(el => !Array.from(el.classList).some(cls => cls.startsWith('active___'))); const _siblingSection = _inactiveSibling || _allSiblings[0]; const _extractClass = (cn, prefixes) => (cn || '').split(/\s+/).filter(x => x && prefixes.some(p => x.startsWith(p))).join(' '); const _neutralLink = _siblingSection ? _siblingSection.querySelector('a') : null; if (_neutralLink) { l.className = _extractClass(_neutralLink.className, ['link___', 'desktopLink', 'mobileLink', 'sidebarMobileLink']); const _sw = _neutralLink.querySelector('[class*="svgIconWrap"]'); const _di = _neutralLink.querySelector('[class*="defaultIcon"]'); const _ln = _neutralLink.querySelector('[class*="linkName"]'); const _liveSvgWrap = _sw ? _extractClass(_sw.className, ['svgIconWrap']) : 'svgIconWrap___AMIqR'; const _liveDefIcon = _di ? _extractClass(_di.className, ['defaultIcon', 'mobile']) : 'defaultIcon___iiNis mobile___paLva'; const _liveLinkName = _ln ? _extractClass(_ln.className, ['linkName']) : 'linkName___FoKha'; l.innerHTML = `<span class="${_liveSvgWrap}"><span class="${_liveDefIcon}">${GYM_LOG_ICON}</span></span>${_ln ? `<span class="${_liveLinkName}">Gym Log</span>` : '<span>Gym Log</span>'}`; } const p = n.closest('.swiper-slide'); if (p) { const s = document.createElement('div'); s.className = cfg.slide || 'swiper-slide slide___se7hj'; s.style.width = n.parentNode.style.width || '43.375px'; s.appendChild(c); const _wr = n.parentNode.parentNode; if (_wr) { _wr.insertBefore(s, n.parentNode.nextSibling); _wr.classList.add('bbgl-swiper-wr'); if (_wr.parentNode) _wr.parentNode.classList.add('bbgl-swiper-cont'); if (!n.parentNode.style.width) { let _woTimer = null; const _wo = new MutationObserver(() => { if (n.parentNode.style.width) { s.style.width = n.parentNode.style.width; _wo.disconnect(); if (_woTimer) { clearTimeout(_woTimer); _woTimer = null; } } }); _wo.observe(n.parentNode, { attributes: true, attributeFilter: ['style'] }); _woTimer = setTimeout(() => { _wo.disconnect(); _woTimer = null; }, 5000); } } } else { n.parentNode.insertBefore(c, n.nextSibling); } }); app.syncSidebarState(); }

function generateDayStartSelect(id, selectedVal = 'utc') { return `<select id="${id}" class="bbgl-native-select"><option value="utc"${selectedVal === 'utc' ? ' selected' : ''}>Torn Time (UTC)</option><option value="local"${selectedVal === 'local' ? ' selected' : ''}>Local Time</option></select>`; }

function buildSection(title, bodyHTML, bodyStyle = '', titleExtraHTML = '') { const style = bodyStyle ? ` style="${bodyStyle}"` : ''; return `<div class="bbgl-prefs-tab-title"><span>${title}</span>${titleExtraHTML}</div><div class="bbgl-settings-body"${style}>${bodyHTML}</div>`; }

function buildRow(labelHTML, controlHTML, extraClass = '') { return `<div class="bbgl-setting-row${extraClass ? ' ' + extraClass : ''}">${labelHTML}${controlHTML}</div>`; }

function buildToggle(id, labelHTML, extraClass = '') { return buildRow(labelHTML, `<label class="bbgl-switch"><input type="checkbox" id="${id}"><span class="slider"></span></label>`, extraClass); }

function buildButton(id, label, modifier = '', extraStyle = '') { const cls = ['bbgl-btn', modifier ? `bbgl-btn-${modifier}` : ''].filter(Boolean).join(' '); const style = extraStyle ? ` style="${extraStyle}"` : ''; return `<button id="${id}" class="${cls}"${style}>${label}</button>`; }

function stackBtnStyle(pos) { return { top: 'border-bottom-left-radius:0; border-bottom-right-radius:0; border-bottom:none;', mid: 'border-radius:0; border-bottom:none;', bottom: 'border-top-left-radius:0; border-top-right-radius:0;' }[pos]; }

function buildApiEntryField(prefix, extraStyle = '') { const style = extraStyle ? ` style="${extraStyle}"` : ''; return `<div class="bbgl-api-container"${style}><div id="${prefix}-api-paste" class="bbgl-paste-icon" data-tooltip="${app.TOOLTIPS.PASTE_CLIPBOARD}">${ICONS.PASTE}</div><input id="${prefix}-api-key" type="text" name="bbgl_api_key" autocomplete="off" class="bbgl-native-input" placeholder="Enter Full or Custom API Key..."></div>`; }

function onChangeLoc(val) { userConfig.buttonLocation = val; saveConfig(); handleDomMutation(); app.syncSiblingSelect('set-loc-select', 'init-loc-select', val); app.syncSiblingSelect('init-loc-select', 'set-loc-select', val); }

function resetSelectionState() { calendarState.selectedData = null; calendarState.selectedLabel = null; viewState.activeViewLabel = null; runtime.stickerData = []; }

function onChangeDayStart(val) { userConfig.dayStartMode = val; saveConfig(); const s = app.getActiveHistory(), baseline = s.meta.baselineBreakdown || ZERO_BREAKDOWN, series = app.DataController.flattenAllSeries(); if (series.length > 0) { const rebuilt = app.DataController._rebuildFromSeries(series, baseline); s.history = rebuilt.history; s.today = rebuilt.today; app.DataController.saveSmartHistory(s); } else app.DataController.invalidate(); resetSelectionState(); app.renderPanelContent(); const tp = dom.topPanel; if (tp && tp.classList.contains('viewing-graph')) app.GraphController.draw(); app.syncSiblingSelect('set-day-start', 'init-day-start', val); app.syncSiblingSelect('init-day-start', 'set-day-start', val); }

function onChangeWeekStart(val) { userConfig.weekStartMode = val; saveConfig(); app.DataController.invalidate(); resetSelectionState(); const wr = dom.panel && dom.panel.querySelector('.bbgl-week-row'); if (wr) { const wd = userConfig.weekStartMode === 'mon' ? ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'] : ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']; wr.innerHTML = wd.map(d => `<span>${d}</span>`).join(''); } app.renderPanelContent(); const tp = dom.topPanel; if (tp && tp.classList.contains('viewing-graph')) app.GraphController.draw(); app.syncSiblingSelect('set-week-start', 'init-week-start', val); app.syncSiblingSelect('init-week-start', 'set-week-start', val); }

const docCache = {};

function refreshInitMask(wv) { const root = wv || dom.welcomeView; if (!root) return; const body = root.querySelector('#init-section-masked-body'); if (!body) return; if (userConfig.privacyAgreed) body.classList.remove('bbgl-mask-active');else body.classList.add('bbgl-mask-active'); refreshInitLock(); }

function refreshInitLock() { if (!dom.panel) return; const isInit = !!localStorage.getItem('bbgl_initialized'); const lock = !isInit && !runtime.demoMode; dom.panel.classList.toggle('bbgl-init-locked', lock); const pc = document.getElementById('bbgl-page-container'); if (pc) pc.classList.toggle('bbgl-init-locked', lock); }

function handleGymClick(e) { const b = e.target.closest('button'); if (!b) return; const l = b.getAttribute('aria-label'); if (!l) return; let id = null; if (l === 'Train strength') id = 5300;else if (l === 'Train defense') id = 5301;else if (l === 'Train speed') id = 5302;else if (l === 'Train dexterity') id = 5303; if (id) { sessionStorage.setItem(KEYS.SESSION, 'true'); if (!runtime.trainDebouncers) runtime.trainDebouncers = {}; if (runtime.trainDebouncers[id]) clearTimeout(runtime.trainDebouncers[id]); runtime.trainDebouncers[id] = setTimeout(() => { app.universalFetch('TRAIN_SINGLE', { specId: id }); runtime.trainDebouncers[id] = null; }, 1000); } }

app.injectWeeklyBar = injectWeeklyBar;
app.getLiveLevelExp = getLiveLevelExp;
app.getLevelBars = getLevelBars;
app.renderLevelBar = renderLevelBar;
app.handleDomMutation = handleDomMutation;
app.settleDomObs = settleDomObs;
app.rearmDomObs = rearmDomObs;
app.SB_DESKTOP = SB_DESKTOP;
app.SB_MOBILE = SB_MOBILE;
app.SB_FLYOUT = SB_FLYOUT;
app.handleLayout = handleLayout;
app.markPanelResizing = markPanelResizing;
app._bbglGetChatRoot = _bbglGetChatRoot;
app._bbglGetChatShoveTargets = _bbglGetChatShoveTargets;
app._bbglIsChatWindow = _bbglIsChatWindow;
app.attachLayoutObservers = attachLayoutObservers;
app.injectGymLevelBar = injectGymLevelBar;
app.injectFooterButton = injectFooterButton;
app.injectSidebarButton = injectSidebarButton;
app.generateDayStartSelect = generateDayStartSelect;
app.buildSection = buildSection;
app.buildRow = buildRow;
app.buildToggle = buildToggle;
app.buildButton = buildButton;
app.stackBtnStyle = stackBtnStyle;
app.buildApiEntryField = buildApiEntryField;
app.onChangeLoc = onChangeLoc;
app.resetSelectionState = resetSelectionState;
app.onChangeDayStart = onChangeDayStart;
app.onChangeWeekStart = onChangeWeekStart;
app.docCache = docCache;
app.refreshInitMask = refreshInitMask;
app.refreshInitLock = refreshInitLock;
app.handleGymClick = handleGymClick;
export { injectWeeklyBar, getLiveLevelExp, getLevelBars, renderLevelBar, handleDomMutation, settleDomObs, rearmDomObs, SB_DESKTOP, handleLayout, markPanelResizing, _bbglGetChatRoot, _bbglGetChatShoveTargets, _bbglIsChatWindow, attachLayoutObservers, injectGymLevelBar, injectFooterButton, injectSidebarButton, generateDayStartSelect, buildSection, buildRow, buildToggle, buildButton, stackBtnStyle, buildApiEntryField, onChangeLoc, resetSelectionState, onChangeDayStart, onChangeWeekStart, docCache, refreshInitMask, refreshInitLock, handleGymClick };
