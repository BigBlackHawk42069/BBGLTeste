import { app } from '../app-context.js';
import { CUSTOM_STICKERS, PAGE_TITLES, cdnize } from '../ui/assets.ts';
import { ASSETS, ICONS } from '../ui/icons.ts';
import { injectStyles } from '../ui/styles.ts';
import {
  ACH_FMT, BACKFILL, BACKFILL_GROUP_KEYS, BACKFILL_GROUP_OF, BACKFILL_GROUPS,
  BASE_DOCS_URL, BBGL_ERROR_CODE, BS_STAT_ROWS, compareVersions, CONSTANTS, ECAN_LOG, ECSTASY_LOG, ENERGY_LOGS, ENERGY_PARAM,
  EX_OD_LOG, GAME, GYM_TIERS, HAPPY_LOGS, ITEM_GROUP_LABELS, ITEM_LOG_META, ITEM_LOGS, KEYS, LAYOUT, LSD_OD_LOG,
  MSG_CLIPBOARD_DENIED, MSG_KEY_FORMAT_INVALID, MSG_KEY_NETWORK_ERROR, MSG_SYNC_NETWORK_ERROR, MSG_SYNC_QUOTA,
  OD_LOGS, r2, SCRIPT_VERSION, STAT_ENHANCER_PARAM, STAT_HAPPY_PARAM, STAT_KEYS, STAT_LOGS, SYNC_FROM_BUFFER,
  TORN_KEY_ERROR_MAP, TRAIN_ENERGY_PARAM, TRAIN_LOGS, WIPE_BELOW_VERSION, XANAX_LOG, XANAX_OD_LOG, ZERO_BREAKDOWN,
  bbglError, tornKeyErrorText
} from '../core/constants.ts';
import { Log, Perf, isDevMode } from '../core/log.ts';
import {
  ALLOWED_CONFIG_KEYS, TAB_ID, calendarState, dom, graphState, historyCache, lastButtonLocation, layoutObservers,
  refreshClickLog, runtime, saveConfig, saveViewState, setHistoryCache, setLastButtonLocation, setTopCeiling, setViewState,
  topCeilingCache, topCeilingTs, userConfig, viewState
} from '../core/state.ts';
import { Formatter, TimeManager, getISOWeek, getWeekKey } from '../domain/time.ts';
import { classifyDay, computeWeekCapsules, computeWeekCompletion, placeCapsuleUnit } from '../domain/capsules.ts';
import { atrophyTitle, calculateLevelProgress, computeDailyLevelExp, computeLevelExpCost } from '../domain/leveling.ts';
import { findHappyJumps, initializeDayObject, normalizeApiLogs, sumStats } from '../domain/day.ts';


function buildSessionText(sl, s, keys) { const statEmoji = { str: '💪', def: '🛡️', spd: '🎯', dex: '🤺' }; const statNames = { str: 'Strength', def: 'Defense', spd: 'Speed', dex: 'Dexterity' }; let ds = ''; if (sl._dailyList && sl._dailyList.length > 1) ds = `${Formatter.dateFull(sl._dailyList[0].date)} - ${Formatter.dateFull(sl._dailyList[sl._dailyList.length - 1].date)}`;else ds = Formatter.dateFull(sl.date); const isSingle = keys.length === 1; const eCost = isSingle ? s[keys[0]].cost : s.total.cost; const eTxt = eCost > 0 ? `⚡${Formatter.number(eCost)} E` : '🛌 I was a lazy POS.'; const statLines = keys.filter(k => s[k].gain > 0 || s[k].cost > 0).map(k => `${statEmoji[k]}${statNames[k]}: +${Formatter.achAbbr(s[k].gain, ACH_FMT.gains)} (${Formatter.achAbbr(s[k].start, ACH_FMT.gains)} \u2192 ${Formatter.achAbbr(s[k].end, ACH_FMT.gains)})`); return ['👑BBGymLog', `${ds} |${eTxt}`, ...statLines].join('\n'); }

function flashCopied(flashEl) { const _flashEls = Array.isArray(flashEl) ? flashEl : [flashEl]; const _states = _flashEls.map(e => { const kids = Array.from(e.children); const visStates = kids.map(c => c.style.visibility); kids.forEach(c => { c.style.visibility = 'hidden'; }); const prevPos = e.style.position; const cs = window.getComputedStyle(e); if (cs.position === 'static') e.style.position = 'relative'; const overlay = document.createElement('span'); overlay.className = 'bbgl-ach-copied-flash'; overlay.textContent = 'Copied!'; e.appendChild(overlay); return { e, kids, visStates, prevPos, overlay }; }); setTimeout(() => _states.forEach(s => { if (s.overlay && s.overlay.parentNode) s.overlay.parentNode.removeChild(s.overlay); s.kids.forEach((c, i) => { c.style.visibility = s.visStates[i]; }); s.e.style.position = s.prevPos; }), 1000); }

function renderStats(sl, rawLbl) { const c = dom.ledgerView; if (!c) return; if (!sl.stats) sl = app.DataController._hydrate(sl, [], rawLbl, 'DAY'); const s = sl.stats, isP = sl.resolution !== 'DAY'; const dEl = dom.dateLabel; if (dEl) { const isExp = dom.panel.classList.contains('bbgl-expanded') || dom.panel.classList.contains('bbgl-mode-page'); let l; if (sl.resolution === 'WEEK') { const start = sl._weekStart || (sl._dailyList && sl._dailyList.length > 0 ? sl._dailyList[0].date : null) || sl.date; const end = sl._weekEnd || (sl._dailyList && sl._dailyList.length > 0 ? sl._dailyList[sl._dailyList.length - 1].date : null) || sl.date; l = `Week of ${Formatter.dateMonthDay(start)}<span class="view-exp"> - ${Formatter.dateMonthDay(end)}</span>`; } else { l = isExp ? Formatter.dateFull(sl.label) : Formatter.datePretty(sl.label); if (!l) l = sl.label; if (sl.resolution === 'MONTH') { l = sl.label + ' ' + calendarState.year; } else if (isP && sl._dailyList.length > 0 && sl.resolution !== 'ALL') { const endLabel = Formatter.dateMonthDay(sl._dailyList[sl._dailyList.length - 1].date); l += `<span class="view-exp"> (${Formatter.dateMonthDay(sl._dailyList[0].date)} - ${endLabel})</span>`; } } dEl.innerHTML = l; } const sumEl = dom.summaryLabel; if (sumEl) sumEl.innerHTML = `Total E: ${Formatter.dual(s.total.cost)} <span style="opacity:0.3; margin:0 6px">|</span> Total Gains: ${Formatter.dual(s.total.gain)}`; const lm = { 'STR': 'Strength', 'DEF': 'Defense', 'SPD': 'Speed', 'DEX': 'Dexterity', 'TOT': 'Total' }; runtime.currentStats = { sl, s }; if (dom.itemCounters) { const items = sl.items || {}; const isDay = sl.resolution === 'DAY'; const cnt = code => items[code] || 0; const shortOf = code => ITEM_LOG_META[code] && ITEM_LOG_META[code].short || `#${code}`; const drugCode = userConfig.drugTracker === 'lsd' ? 2230 : XANAX_LOG; const secondaryCode = userConfig.drugTracker === 'lsd' ? XANAX_LOG : 2230; const parts = []; let drugSub = ''; if (!isDay) { const days = app.DataController.periodCalendarDays(sl); const drugAvg = days > 0 ? cnt(drugCode) / days : 0; drugSub = sl.resolution === 'ALL' ? '' : `<span class="bbgl-ic-sub">(${drugAvg.toFixed(2)})</span>`; } const nameOf = c => { if (c === 2290) return 'Xanax'; if (c === 2230) return 'LSD'; if (c === 2040) return 'Cans'; if (c === 2190) return 'FHC'; if (c === 8981) return 'Eggs'; return shortOf(c); }; const isAll = sl.resolution === 'ALL'; const drugTip = `<div style="text-align:center">${nameOf(drugCode)} Taken` + (!isDay && !isAll ? `<br><span class="tt-sub">(Avg/Day)</span>` : ``) + `</div>`; parts.push(`<span class="bbgl-ic" data-tooltip-html='${drugTip}'>${shortOf(drugCode)}: ${cnt(drugCode)}${drugSub}</span>`); [ECAN_LOG, 2190, secondaryCode, 8981].forEach(code => { const c = cnt(code); if (c <= 0) return; const sub = code === ECAN_LOG && sl.resolution !== 'ALL' ? `<span class="bbgl-ic-sub">(+${Math.round(sl.ecanEnergy || 0)})</span>` : ''; let dynTip = ''; if (code === ECAN_LOG) { dynTip = `<div style="text-align:center">Cans Used` + (!isAll ? `<br><span class="tt-sub">(Energy Gained)</span>` : ``) + `</div>`; } else { dynTip = `<div style="text-align:center">${nameOf(code)} Used</div>`; } parts.push(`<span class="bbgl-ic bbgl-ic-dyn" data-tooltip-html='${dynTip}'>${shortOf(code)}: ${c}${sub}</span>`); }); const refills = cnt(4900); const refillVal = isDay ? refills > 0 ? `<span class="bbgl-ic-yes">✓</span>` : `<span class="bbgl-ic-no">✗</span>` : `${refills}`; const refillTip = `<div style="text-align:center">Refills Used</div>`; parts.push(`<span class="bbgl-ic" data-tooltip-html='${refillTip}'>Refill: ${refillVal}</span>`); dom.itemCounters.innerHTML = parts.join(''); } const todayStr = Formatter.dateLogical(); const slLastDate = sl._dailyList && sl._dailyList.length > 0 ? sl._dailyList[sl._dailyList.length - 1].date : sl.date; const isCurrentPeriod = sl.resolution === 'ALL' || slLastDate >= todayStr; const col = (lc, k, cl) => { const d = s[k], ft = lm[lc] || lc; let rh = '', rt = ''; const fmtR = n => { if (!n && n !== 0) return '0'; const a = Math.abs(n); if (a >= 1e15) return (n / 1e15).toFixed(4) + 'q'; if (a >= 1e12) return (n / 1e12).toFixed(4) + 't'; if (a >= 1e9) return (n / 1e9).toFixed(4) + 'b'; if (a >= 100) return Math.round(n).toLocaleString('en-US'); return n.toFixed(1); }; const mkTip = (r1, r2, pct, sg) => `<div style='text-align:center;line-height:1.6'><div style='margin-bottom:0px'>Growth Rate</div><div style='font-size:0.85em;opacity:0.35;margin-bottom:3px'>(Gains/150E)</div><div>${fmtR(r1)} \u2192 ${fmtR(r2)}</div><div style='font-size:0.85em;color:#aaa'>${sg}${Math.round(pct)}%</div></div>`; if (isP && k !== 'total') { let th = `<span style="opacity:0.3">--</span>`; if (userConfig.ratesEnabled && sl._dailyList.length > 0) { const _fpd = new Date(sl._dailyList[0].date + 'T00:00:00Z'); _fpd.setUTCDate(_fpd.getUTCDate() - 1); const r1 = app.DataController.getHistoricalRate(_fpd.toISOString().slice(0, 10), k),
    r2 = app.DataController._hydrate(sl._dailyList[sl._dailyList.length - 1], [], '', 'DAY').stats[k].rate, del = r2 - r1, sg = del >= 0 ? '+' : '', pct = r1 > 0 ? (r2 - r1) / r1 * 100 : 0; th = `<div class="rates-group" style="display:flex;flex-direction:column;align-items:center;line-height:1.1"><span>${sg}${Formatter.achAbbr(del, ACH_FMT.compact)}</span><span class="view-exp rate-pct" style="font-size:0.8em;opacity:0.7;margin-top:2px;margin-bottom:-2px;">(${sg}${Formatter.ratePct(pct)}%)</span></div>`; rt = mkTip(r1, r2, pct, sg); } rh = userConfig.ratesEnabled ? th : ''; } else { if (userConfig.ratesEnabled && k !== 'total') { const _pd = new Date(sl.date + 'T00:00:00Z'); _pd.setUTCDate(_pd.getUTCDate() - 1); const r1 = app.DataController.getHistoricalRate(_pd.toISOString().slice(0, 10), k), r2 = d.rate, del = r2 - r1, sg = del >= 0 ? '+' : '', pct = r1 > 0 ? del / r1 * 100 : 0; rh = userConfig.ratesEnabled ? Formatter.dual(d.rate, true) : ''; rt = mkTip(r1, r2, pct, sg); } else { rh = userConfig.ratesEnabled ? Formatter.dual(d.rate, true) : ''; rt = `Growth Rate (Gains / 150E)`; } } return `<div class="stat-column" data-copy-stat="${k}"><div class="col-header cell-stack"><div class="l-top c-label ${cl} bbgl-copy-label" data-tooltip="Click to copy ${ft} data" style="cursor:pointer"><span class="view-std">${lc}</span><span class="view-exp">${ft}</span></div><div class="l-bot" data-tooltip="${isP ? `Energy Used on ${ft}` : `Energy Used`}">${Formatter.dual(d.cost)} E</div></div><div class="bbgl-spacer"></div><div class="col-data-block cell-stack c-gain"><div class="l-top" data-tooltip="${ft} Gained">+${Formatter.dual(d.gain)}</div><div class="l-bot" data-tooltip="${rt}">${rh}</div></div><div class="bbgl-spacer"></div><div class="col-data-block cell-stack c-total"><div class="l-top" data-tooltip="${isCurrentPeriod ? 'Current' : 'Ending'} ${ft}">${Formatter.dual(d.end)}</div><div class="l-bot" data-tooltip="Starting ${ft}">${Formatter.dual(d.start)}</div></div></div>`; }; c.innerHTML = ` ${col('STR', 'str', 't-str')} ${col('DEF', 'def', 't-def')} ${col('SPD', 'spd', 't-spd')} ${col('DEX', 'dex', 't-dex')} `; }

app.buildSessionText = buildSessionText;
app.flashCopied = flashCopied;
app.renderStats = renderStats;
export { buildSessionText, flashCopied, renderStats };
