import { app } from '../app-context.js';
import {
  ACH_FMT, BACKFILL, BACKFILL_GROUP_KEYS, BACKFILL_GROUP_OF, BACKFILL_GROUPS,
  BASE_DOCS_URL, BBGL_ERROR_CODE, BS_STAT_ROWS, compareVersions, CONSTANTS, ECAN_LOG, ECSTASY_LOG, ENERGY_LOGS, ENERGY_PARAM,
  EX_OD_LOG, GAME, GYM_TIERS, HAPPY_LOGS, ITEM_GROUP_LABELS, ITEM_LOG_META, ITEM_LOGS, KEYS, LAYOUT, LSD_OD_LOG,
  MSG_CLIPBOARD_DENIED, MSG_KEY_FORMAT_INVALID, MSG_KEY_NETWORK_ERROR, MSG_SYNC_NETWORK_ERROR, MSG_SYNC_QUOTA,
  OD_LOGS, r2, SCRIPT_VERSION, STAT_ENHANCER_PARAM, STAT_HAPPY_PARAM, STAT_KEYS, STAT_LOGS, SYNC_FROM_BUFFER,
  TORN_KEY_ERROR_MAP, TRAIN_ENERGY_PARAM, TRAIN_LOGS, WIPE_BELOW_VERSION, XANAX_LOG, XANAX_OD_LOG, ZERO_BREAKDOWN,
  bbglError, tornKeyErrorText
} from '../core/constants.ts';
import { Log, Perf } from '../core/log.ts';
import {
  ALLOWED_CONFIG_KEYS, TAB_ID, calendarState, dom, graphState, historyCache, lastButtonLocation, layoutObservers,
  refreshClickLog, runtime, saveConfig, saveViewState, setHistoryCache, setLastButtonLocation, setTopCeiling, setViewState,
  topCeilingCache, topCeilingTs, userConfig, viewState
} from '../core/state.ts';
import { Formatter, TimeManager, getISOWeek, getWeekKey } from '../domain/time.ts';
import { calculateLevelProgress, computeLevelExpCost } from '../domain/leveling.ts';


function renderPanelContent() { const s = app.getActiveHistory(), dm = app.DataController.getDateMap(), tk = Formatter.dateLogical(); if ((s.today.startTotal > 0 || s.today.date) && !dm[tk]) dm[tk] = s.today; const c = dom.calContainer; if (!c) return; Perf.start('renderPanel'); c.innerHTML = ''; const y = calendarState.year, m = calendarState.month, yt = dom.yearTrigger; dom.monthTrigger.textContent = CONSTANTS.MONTHS[m]; yt.textContent = y; yt.classList.remove('disabled'); let f = new Date(y, m, 1), start = f.getDay(); if (start === -1) start = 6; if (userConfig.weekStartMode === 'mon') start = start === 0 ? 6 : start - 1; const dim = new Date(y, m + 1, 0).getDate(), dipm = new Date(y, m, 0).getDate(); let pm = m - 1, py = y; if (pm < 0) { pm = 11; py--; } let cells = []; for (let i = 0; i < start; i++) cells.push({ y: py, m: pm, d: dipm - start + i + 1, g: true }); for (let d = 1; d <= dim; d++) cells.push({ y: y, m: m, d: d, g: false }); let rem = 7 - cells.length % 7; if (rem < 7 && rem > 0) { let nm = m + 1, ny = y; if (nm > 11) { nm = 0; ny++; } for (let i = 1; i <= rem; i++) cells.push({ y: ny, m: nm, d: i, g: true }); } calendarState.visibleCells = cells.map(z => Formatter.dateISO(z.y, z.m, z.d)); c.style.setProperty('--total-rows', 6); c.style.setProperty('--bg-url', `url(${app.CAL_IMG_BASE}cal-grid-futr.jpg)`); const todayStr = Formatter.dateLogical(); const frag = document.createDocumentFragment(); let batch = [], ridx = 0; cells.forEach(function tickCalendarCell(z) { const ds = Formatter.dateISO(z.y, z.m, z.d), d = dm[ds] || null; batch.push({ ...z, p: d }); if (batch.length === 7) { const rd = document.createElement('div'), last = batch[6], weekEndStr = Formatter.dateISO(last.y, last.m, last.d), isArch = weekEndStr < todayStr; rd.className = 'bbgl-row-slice' + (isArch ? ' bbgl-row-archived' : ''); rd.style.setProperty('--row-idx', ridx); if (isArch) rd.style.setProperty('--bg-url', `url(${app.CAL_IMG_BASE}cal-grid-past.jpg)`); let wdb = []; batch.forEach(function tickWeekCell(i, cIdx) { app.renderCell(rd, i.y, i.m, i.d, i.g, ridx, cIdx); wdb.push({ date: Formatter.dateISO(i.y, i.m, i.d), data: i.p }); }); frag.appendChild(rd); app.injectWeeklyBar(frag, wdb); batch = []; ridx++; } }); c.appendChild(frag); if (runtime._pendingHistoryRestore) { const { sl, label } = runtime._pendingHistoryRestore; runtime._pendingHistoryRestore = null; openHistory(sl, label); } const tp = dom.topPanel; if (tp) { if (tp.classList.contains('viewing-graph')) app.GraphController.draw();else if (tp.classList.contains('viewing-stickers')) app.renderStickers();else if (tp.classList.contains('viewing-achievements')) app.renderAchievements(); } if (!calendarState.selectedData) app.renderStats(app.DataController.getSlice('DAY', Formatter.dateLogical()), Formatter.dateLogical());else app.renderStats(calendarState.selectedData, calendarState.selectedLabel); Perf.end('renderPanel'); updateLevelBar(); app.updateSummaryCharts(); }

function updateLevelBar() { const totalExp = app.getLiveLevelExp(); if (runtime._lastLevelExp === undefined) { runtime._lastLevelExp = totalExp; const bars = app.getLevelBars(); bars.forEach(b => app.renderLevelBar(b, totalExp)); return; } const bars = app.getLevelBars(); if (!bars.length) return; if (totalExp !== runtime._lastLevelExp) { runtime._targetLevelExp = totalExp; if (!runtime._isAnimatingLevel) { runLevelAnimationQueue(); } } else if (!runtime._isAnimatingLevel) { bars.forEach(b => { if (!b.fill.style.width) app.renderLevelBar(b, runtime._lastLevelExp); }); } async function runLevelAnimationQueue() { runtime._isAnimatingLevel = true; const BASE_SPEED_MS = 1000; let forcedNextTier = null; while (runtime._lastLevelExp < runtime._targetLevelExp) { const currentProg = forcedNextTier !== null ? { atrophy: forcedNextTier, level: 1, expInLevel: 0, expToNext: computeLevelExpCost(1, forcedNextTier) } : calculateLevelProgress(runtime._lastLevelExp); forcedNextTier = null; const targetProg = calculateLevelProgress(runtime._targetLevelExp); const currentRank = currentProg.atrophy * 100 + currentProg.level; const targetRank = targetProg.atrophy * 100 + targetProg.level; if (currentRank < targetRank && currentProg.level >= 100 && currentProg.atrophy < 2) { await runAtrophyAnimation(currentProg.atrophy, bars); forcedNextTier = currentProg.atrophy + 1; } else if (currentRank < targetRank) { const expNeededToFill = currentProg.expToNext - currentProg.expInLevel; const currentPct = parseFloat(bars[0].fill.style.width) || 0; const durationMs = Math.max(150, (100 - currentPct) / 100 * BASE_SPEED_MS); bars.forEach(b => { b.fill.style.transitionDuration = durationMs + 'ms'; b.fill.style.width = '96.8%'; b.fill.classList.add('level-full'); }); await new Promise(r => setTimeout(r, durationMs + 50)); bars.forEach(b => b.container.classList.add('bbgl-level-up-flash')); await new Promise(r => setTimeout(r, 200)); const nextLevel = currentProg.level + 1; bars.forEach(b => { b.num.innerHTML = '<span class="bbgl-lv-prefix">Lv </span>' + nextLevel; }); await new Promise(r => setTimeout(r, 650)); bars.forEach(b => b.container.classList.remove('bbgl-level-up-flash')); runtime._lastLevelExp += expNeededToFill; if (nextLevel >= 100 && currentProg.atrophy < 2) { await runAtrophyAnimation(currentProg.atrophy, bars); forcedNextTier = currentProg.atrophy + 1; } else { bars.forEach(b => { b.fill.style.transition = 'none'; b.fill.style.width = '0%'; b.fill.classList.remove('level-full'); void b.fill.offsetWidth; b.fill.style.transition = ''; }); } } else { runtime._lastLevelExp = runtime._targetLevelExp; const currentPct = parseFloat(bars[0].fill.style.width) || 0; const { level, expInLevel, expToNext } = calculateLevelProgress(runtime._lastLevelExp); const targetPct = expToNext > 0 ? Math.min(100, expInLevel / expToNext * 100) : level >= 100 ? 100 : 0; const durationMs = Math.max(150, Math.abs(targetPct - currentPct) / 100 * BASE_SPEED_MS); bars.forEach(b => { b.fill.style.transitionDuration = durationMs + 'ms'; app.renderLevelBar(b, runtime._lastLevelExp); }); await new Promise(r => setTimeout(r, durationMs + 50)); } } bars.forEach(b => { b.fill.style.transitionDuration = ''; }); runtime._lastLevelExp = runtime._targetLevelExp; runtime._isAnimatingLevel = false; } }

function snapLevelBar() { const totalExp = app.getLiveLevelExp(); runtime._lastLevelExp = totalExp; runtime._targetLevelExp = totalExp; runtime._isAnimatingLevel = false; app.getLevelBars().forEach(b => { b.fill.style.transition = 'none'; b.container.classList.remove('bbgl-level-up-flash'); app.renderLevelBar(b, totalExp); void b.fill.offsetWidth; b.fill.style.transition = ''; }); }

async function runAtrophyAnimation(fromAtrophy, bars) { const toAtrophy = fromAtrophy + 1; if (!userConfig.animations) { bars.forEach(b => { b.container.dataset.atrophy = toAtrophy; b.container.dataset.level = 1; b.num.textContent = 'Lv 1'; b.fill.style.transition = 'none'; b.fill.style.width = '0%'; b.fill.classList.remove('level-full'); void b.fill.offsetWidth; b.fill.style.transition = ''; }); if (dom.panel) { dom.panel.dataset.atrophy = toAtrophy; dom.panel.dataset.level = 1; } return; } const TUCK_MS = 350; const RISE_MS = 900; const FLASH_MS = 700; bars.forEach(b => b.container.classList.add('bbgl-crown-tuck')); await new Promise(r => setTimeout(r, TUCK_MS)); bars.forEach(b => { b.container.classList.remove('bbgl-crown-tuck'); b.container.dataset.atrophy = toAtrophy; b.container.classList.add('bbgl-crown-rise'); }); if (dom.panel) dom.panel.dataset.atrophy = toAtrophy; await new Promise(r => setTimeout(r, RISE_MS)); bars.forEach(b => b.container.classList.add('bbgl-atrophied-flash')); await new Promise(r => setTimeout(r, FLASH_MS)); bars.forEach(b => { b.container.classList.remove('bbgl-crown-rise', 'bbgl-atrophied-flash'); b.container.dataset.level = 1; b.num.textContent = 'Lv 1'; b.fill.style.transition = 'none'; b.fill.style.width = '0%'; b.fill.classList.remove('level-full'); void b.fill.offsetWidth; b.fill.style.transition = ''; }); if (dom.panel) dom.panel.dataset.level = 1; }

function updateCellSelection(newLabel) { const c = dom.calContainer; if (!c) return; c.querySelectorAll('.bbgl-day-cell.is-viewing').forEach(el => { el.classList.remove('is-viewing'); if (!el.matches(':hover')) el.classList.remove('shimmer-active'); }); c.querySelectorAll('.bbgl-weekly-track.is-viewing').forEach(el => el.classList.remove('is-viewing')); if (!newLabel) { const today = document.getElementById('active-date-today'); if (today) { today.classList.add('is-viewing'); if (today._buildShine) today._buildShine(); } return; } const dC = c.querySelector(`.bbgl-day-cell[data-date="${newLabel}"]`); if (dC) { dC.classList.add('is-viewing'); if (userConfig.animations && !dC.classList.contains('shimmer-active')) dC.classList.add('shimmer-active'); if (dC._buildShine) dC._buildShine(); return; } const track = c.querySelector(`.bbgl-weekly-track[data-label="${newLabel}"]`); if (track) track.classList.add('is-viewing'); }

function openHistory(d, l) { if (runtime.isViewAnimating) { dom.ledgerView.classList.remove('bbgl-crt-out', 'bbgl-crt-in'); runtime.isViewAnimating = false; } viewState.activeViewLabel = l; saveViewState(); if (calendarState.selectedLabel === l && !dom.topPanel.classList.contains('viewing-graph')) return; runtime.isViewAnimating = true; calendarState.selectedData = d; calendarState.selectedLabel = l; const mBtn = document.getElementById('month-stats-btn'); const yBtn = document.getElementById('year-stats-btn'); const aBtn = document.getElementById('all-time-btn'); if (mBtn) mBtn.classList.toggle('active', l === CONSTANTS.MONTHS[calendarState.month]); if (yBtn) yBtn.classList.toggle('active', l === String(calendarState.year)); if (aBtn) aBtn.classList.toggle('active', l === 'All-Time'); app.closeItemViewer(); updateCellSelection(l); const tp = dom.topPanel; if (tp.classList.contains('viewing-stickers')) { app.switchView('ledger'); setTimeout(() => { app.renderStats(d, l); }, 300); return; } if (tp.classList.contains('viewing-graph')) { app.GraphController.draw(); const de = dom.dateLabel; if (de) de.innerText = Formatter.datePretty(l) || l; runtime.isViewAnimating = false; } else { if (viewState.achEnhPeriodMode && tp.classList.contains('viewing-achievements')) { app.achRefreshPageDom(); runtime.isViewAnimating = false; return; } const el = dom.ledgerView; if (userConfig.animations) { el.classList.add('bbgl-crt-out'); setTimeout(() => { el.classList.remove('bbgl-crt-out'); app.renderStats(d, l); el.classList.add('bbgl-crt-in'); setTimeout(() => { el.classList.remove('bbgl-crt-in'); runtime.isViewAnimating = false; }, 300); }, 280); } else { app.renderStats(d, l); runtime.isViewAnimating = false; } } }

function closeHistory(e) { if (e) e.stopPropagation(); if (!calendarState.selectedData) return; if (runtime.isViewAnimating) { dom.ledgerView.classList.remove('bbgl-crt-out', 'bbgl-crt-in'); runtime.isViewAnimating = false; } viewState.activeViewLabel = null; saveViewState(); runtime.isViewAnimating = true; calendarState.selectedData = null; calendarState.selectedLabel = null; updateCellSelection(null); const tp = dom.topPanel, ts = Formatter.dateLogical(); if (tp.classList.contains('viewing-graph')) { app.GraphController.draw(); const de = dom.dateLabel; if (de) de.innerText = Formatter.datePretty(ts) || ts; runtime.isViewAnimating = false; } else if (tp.classList.contains('viewing-stickers')) runtime.isViewAnimating = false;else { const el = dom.ledgerView; if (userConfig.animations) { el.classList.add('bbgl-crt-out'); setTimeout(() => { el.classList.remove('bbgl-crt-out'); app.renderStats(app.getActiveHistory().today, ts); el.classList.add('bbgl-crt-in'); setTimeout(() => { el.classList.remove('bbgl-crt-in'); runtime.isViewAnimating = false; }, 300); }, 280); } else { app.renderStats(app.getActiveHistory().today, ts); runtime.isViewAnimating = false; } } }

function changeMonth(d) { const c = dom.calContainer; if (!c) return; let m = calendarState.month + d, y = calendarState.year; if (m > 11) { m = 0; y++; } if (m < 0) { m = 11; y--; } if (!userConfig.animations) { calendarState.month = m; calendarState.year = y; viewState.calYear = y; viewState.calMonth = m; saveViewState(); renderPanelContent(); return; } c.parentElement.querySelectorAll('.bbgl-cal-ghost').forEach(g => g.remove()); const ghost = c.cloneNode(true); ghost.className += ' bbgl-cal-ghost'; ghost.style.animation = d > 0 ? 'bbgl-slide-out-l 0.3s ease forwards' : 'bbgl-slide-out-r 0.3s ease forwards'; c.parentElement.appendChild(ghost); const removeGhost = () => { if (ghost.parentElement) ghost.remove(); }; ghost.addEventListener('animationend', removeGhost, { once: true }); const ghostTimer = setTimeout(removeGhost, 400); ghost.addEventListener('animationend', () => clearTimeout(ghostTimer), { once: true }); calendarState.month = m; calendarState.year = y; viewState.calYear = y; viewState.calMonth = m; saveViewState(); c.style.willChange = 'transform'; renderPanelContent(); c.style.animation = d > 0 ? 'bbgl-slide-in-r 0.3s ease forwards' : 'bbgl-slide-in-l 0.3s ease forwards'; c.addEventListener('animationend', () => { c.style.animation = ''; c.style.willChange = 'auto'; }, { once: true }); }

function calcAllTimeStats() { const sl = app.DataController.getSlice('ALL', 'All-Time'); openHistory(sl, 'All-Time'); }

function calcPeriodStats(t) { const lbl = t === 'month' ? CONSTANTS.MONTHS[calendarState.month] : String(calendarState.year), m = t === 'month' ? 'MONTH' : 'YEAR', sl = app.DataController.getSlice(m, lbl, calendarState.year); openHistory(sl, lbl); }

app.renderPanelContent = renderPanelContent;
app.updateLevelBar = updateLevelBar;
app.snapLevelBar = snapLevelBar;
app.runAtrophyAnimation = runAtrophyAnimation;
app.updateCellSelection = updateCellSelection;
app.openHistory = openHistory;
app.closeHistory = closeHistory;
app.changeMonth = changeMonth;
app.calcAllTimeStats = calcAllTimeStats;
app.calcPeriodStats = calcPeriodStats;
export { renderPanelContent, updateLevelBar, snapLevelBar, runAtrophyAnimation, updateCellSelection, openHistory, closeHistory, changeMonth, calcAllTimeStats, calcPeriodStats };
