import { app } from '../app-context.js';
import { CUSTOM_STICKERS, PAGE_TITLES, cdnize } from '../ui/assets.ts';
import { ASSETS, ICONS } from '../ui/icons.ts';
import {
  ACH_FMT, BACKFILL, BACKFILL_GROUP_KEYS, BACKFILL_GROUP_OF, BACKFILL_GROUPS,
  BASE_DOCS_URL, BBGL_ERROR_CODE, BS_STAT_ROWS, compareVersions, CONSTANTS, ECAN_LOG, ECSTASY_LOG, ENERGY_LOGS, ENERGY_PARAM,
  EX_OD_LOG, GAME, GYM_TIERS, HAPPY_LOGS, ITEM_GROUP_LABELS, ITEM_LOG_META, ITEM_LOGS, KEYS, LAYOUT, LSD_OD_LOG,
  MSG_CLIPBOARD_DENIED, MSG_KEY_FORMAT_INVALID, MSG_KEY_NETWORK_ERROR, MSG_SYNC_NETWORK_ERROR, MSG_SYNC_QUOTA,
  OD_LOGS, r2, SCRIPT_VERSION, STAT_ENHANCER_PARAM, STAT_HAPPY_PARAM, STAT_KEYS, STAT_LOGS, SYNC_FROM_BUFFER,
  TORN_KEY_ERROR_MAP, TRAIN_ENERGY_PARAM, TRAIN_LOGS, WIPE_BELOW_VERSION, XANAX_LOG, XANAX_OD_LOG, ZERO_BREAKDOWN,
  bbglError, tornKeyErrorText
} from '../core/constants.ts';
import { Log, Perf, isDevMode } from '../core/log.ts';
import {
  ALLOWED_CONFIG_KEYS, TAB_ID, calendarState, dom, graphState, historyCache, lastButtonLocation, layoutObservers,
  refreshClickLog, runtime, saveConfig, saveViewState, setHistoryCache, setLastButtonLocation, setTopCeiling, setViewState,
  topCeilingCache, topCeilingTs, userConfig, viewState
} from '../core/state.ts';
import { Formatter, TimeManager, getISOWeek, getWeekKey } from '../domain/time.ts';
import { classifyDay, computeWeekCapsules, computeWeekCompletion, placeCapsuleUnit } from '../domain/capsules.ts';
import { atrophyTitle, calculateLevelProgress, computeDailyLevelExp, computeLevelExpCost } from '../domain/leveling.ts';
import { findHappyJumps, initializeDayObject, normalizeApiLogs, sumStats } from '../domain/day.ts';


function generateDemoData() { let _seed = 0x9e3779b9; function rand() { _seed += 0x6d2b79f5; let t = _seed; t = Math.imul(t ^ t >>> 15, t | 1); t ^= t + Math.imul(t ^ t >>> 7, t | 61); return ((t ^ t >>> 14) >>> 0) / 4294967296; } function randInt(lo, hi) { return lo + Math.floor(rand() * (hi - lo + 1)); } const today = Formatter.dateLogical(); const todayMs = Formatter.parse(today).getTime(); const DAY_MS = 86400000; const NUM_DAYS = 365; const Simulation = { A: 3.480061091e-7, B: 250, C: 3.091619094e-6, D: 6.82775184551527e-5, E: -0.0301431777 }; const DEMO_GYM_DOTS = 9.0; const DEMO_HAPPY = 4950; const DEMO_MODIFIERS = 2.5; const DEMO_E_PER_TRAIN = 5; const DEMO_FORMULA_E_BASE = 10; function simulationGain(statTotal) { const happyFactor = DEMO_HAPPY + Simulation.B; const base = (Simulation.A * Math.log(happyFactor) + Simulation.C) * statTotal + Simulation.D * happyFactor + Simulation.E; const perStandardTrain = base * DEMO_GYM_DOTS * DEMO_MODIFIERS; const perTrain = perStandardTrain * (DEMO_E_PER_TRAIN / DEMO_FORMULA_E_BASE); return Math.max(0, perTrain); } const statKeys = ['str', 'def', 'spd', 'dex']; const dates = []; for (let i = NUM_DAYS - 1; i >= 0; i--) { const ms = todayMs - i * DAY_MS; const d = new Date(ms); dates.push(Formatter.dateISO(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate())); } const baseline = {}; statKeys.forEach(k => { baseline[k] = 15000 + randInt(0, 10000); }); const weekStartOffset = userConfig.weekStartMode === 'mon' ? 1 : 0; const todayDate = new Date(todayMs); const todayDow = todayDate.getUTCDay(); const daysFromWeekStart = (todayDow - weekStartOffset + 7) % 7; const weekDay0Str = dates[NUM_DAYS - 1 - daysFromWeekStart] || null; const weekDay1Str = daysFromWeekStart >= 1 ? dates[NUM_DAYS - daysFromWeekStart] : null; const running = { ...baseline }; const history = []; let mixedWeek = 0; dates.forEach((dateStr, idx) => { const roll = rand(); if (roll < 0.10) return; if (mixedWeek === 0 && rand() < 0.08) mixedWeek = 7; let eTotalRaw; if (mixedWeek > 0) { mixedWeek--; const subRoll = rand(); if (subRoll < 0.25) { eTotalRaw = randInt(200, 260) * 10; } else if (subRoll < 0.55) { eTotalRaw = randInt(150, 190) * 10; } else if (subRoll < 0.85) { eTotalRaw = randInt(100, 149) * 10; } else { eTotalRaw = randInt(50, 99) * 10; } } else if (roll < 0.15) { eTotalRaw = randInt(70, 99) * 10; } else { eTotalRaw = randInt(100, 160) * 10; } if (dateStr === weekDay0Str) eTotalRaw = Math.max(eTotalRaw, 2500);else if (dateStr === weekDay1Str && eTotalRaw < 2000) eTotalRaw = Math.min(Math.max(eTotalRaw, 1000), 1499);else if (rand() < 0.05) eTotalRaw = Math.max(eTotalRaw, 2500); const hjRoll = rand(); const isDiamondHJDay = hjRoll < 0.05; const isHJDay = isDiamondHJDay || hjRoll < 0.18; if (isDiamondHJDay && eTotalRaw < 1800) eTotalRaw = randInt(180, 220) * 10;else if (isHJDay && eTotalRaw < 1000) eTotalRaw = randInt(100, 170) * 10; const hjWindowStart = isHJDay ? randInt(36000, 36000 + 28800 - GAME.HJ_WINDOW_SECONDS) : null; const eTotal = eTotalRaw; const numStats = randInt(1, 4); const chosenStats = [...statKeys].sort(() => rand() - 0.5).slice(0, numStats); const ePerStat = {}; let eRemain = eTotal; chosenStats.forEach((k, i) => { const share = i === chosenStats.length - 1 ? eRemain : Math.round((rand() * 0.4 + 0.1) * eTotal / numStats) * 10 || 10; ePerStat[k] = Math.max(10, Math.min(share, eRemain)); eRemain -= ePerStat[k]; }); if (eRemain > 0 && chosenStats.length) ePerStat[chosenStats[0]] += eRemain; const startBreakdown = { ...running }; const eSpent = { total: eTotal, ...ZERO_BREAKDOWN }; const gains = { total: 0, ...ZERO_BREAKDOWN }; const series = []; const dayStartSec = Math.floor(Formatter.parse(dateStr).getTime() / 1000); chosenStats.forEach(k => { const cost = ePerStat[k] || 0; if (!cost) return; const trainsForStat = Math.floor(cost / DEMO_E_PER_TRAIN); let statGainAccum = 0; for (let t = 0; t < trainsForStat; t++) { const raw = simulationGain(running[k]); const jittered = raw * (0.97 + rand() * 0.06); running[k] += jittered; statGainAccum += jittered; const ts = hjWindowStart !== null ? dayStartSec + hjWindowStart + Math.floor(rand() * GAME.HJ_WINDOW_SECONDS) : dayStartSec + 36000 + Math.floor((t + rand()) * (28800 / Math.max(1, trainsForStat))); series.push({ ts, stat: k, gain: Math.round(jittered), cost: DEMO_E_PER_TRAIN, after: Math.round(running[k]), rate: r2(Math.round(jittered) / DEMO_E_PER_TRAIN * 150), synthetic: true }); } eSpent[k] = cost; const roundedStatGain = Math.round(statGainAccum); gains[k] = roundedStatGain; gains.total += roundedStatGain; }); eSpent.total = eSpent.str + eSpent.def + eSpent.spd + eSpent.dex; const endBreakdown = { ...running }; const day = initializeDayObject(dateStr,
    startBreakdown); day.gains = gains; day.eSpent = eSpent; day.endBreakdown = endBreakdown; day.endTotal = endBreakdown.str + endBreakdown.def + endBreakdown.spd + endBreakdown.dex; day.series = series; day.lastLogTimestamp = series.length ? series[series.length - 1].ts : 0; history.push(day); }); const lastHistDay = history.length ? history[history.length - 1] : null; const todayStart = lastHistDay ? { ...lastHistDay.endBreakdown } : { ...running }; const todayObj = initializeDayObject(today, todayStart); const oldestDate = history.length ? history[0].date : today; const logStartDate = Math.floor(Formatter.parse(oldestDate).getTime() / 1000); const lastRates = {}; statKeys.forEach(k => { const perFiveE = simulationGain(running[k]); lastRates[k] = perFiveE * (DEMO_FORMULA_E_BASE / DEMO_E_PER_TRAIN); }); const meta = { baselineBreakdown: { ...baseline }, logStartDate }; return { meta, history, today: todayObj }; }

function refreshDemoMasks() { if (!dom.settingsView) return; dom.settingsView.querySelectorAll('.bbgl-demo-maskable').forEach(el => { el.classList.toggle('bbgl-mask-active', !!runtime.demoMode); }); const sdemo = dom.settingsView.querySelector('#settings-demo-btn'); if (sdemo) sdemo.innerText = runtime.demoMode ? 'EXIT DEMO' : 'DEMO MODE'; }

function enterDemo(source) { if (source === 'settings' || source === 'privacy') { runtime.realReturnView = runtime.returnView; } localStorage.setItem(KEYS.DEMO, '1'); runtime.demoMode = true; runtime.demoHistory = null; runtime.stickerData = []; setHistoryCache(null); app.DataController.invalidate(); calendarState.selectedData = null; calendarState.selectedLabel = Formatter.dateLogical(); viewState.activeViewLabel = null; const deb = dom.panel ? dom.panel.querySelector('#bbgl-demo-exit') : null; if (deb) deb.style.display = 'flex'; const debBtn = dom.panel ? dom.panel.querySelector('#bbgl-demo-exit-btn') : null; if (debBtn) debBtn.style.display = 'flex'; const pdeb = document.getElementById('bbgl-page-demo-exit'); if (pdeb) pdeb.style.display = 'flex'; app.refreshInitLock(); refreshDemoMasks(); app.snapLevelBar(); app.switchView('ledger'); }

function enterDemoFromSettings() { enterDemo('settings'); }

function buildWelcomeIntroSection() { const body = `<div id="bbgl-welcome-intro-text">${app.DOC_LOADING_HTML}</div>${app.buildButton('init-privacy-btn', 'PRIVACY DISCLOSURE', '', 'margin:0 10px 8px 10px; width: calc(100% - 20px); display:block;')}`; return `<div class="bbgl-prefs-tab-title" style="border-radius:5px 5px 0 0; margin-top:0;">Welcome to Big Black Gym Log</div><div class="bbgl-settings-body" style="margin-bottom:5px;">${body}</div>`; }

function buildWelcomeInitSection() { const inputHTML = app.buildApiEntryField('init', 'margin:8px 10px;'); const createBtn = app.buildButton('init-create-api-btn', 'CREATE API KEY', '', 'margin:0 10px 8px 10px; width: calc(100% - 20px); display:block;'); const rows = app.buildRow(`<span data-tooltip-html="${app.TOOLTIPS.DAY_START}">Log Timezone</span>`, app.generateDayStartSelect('init-day-start', userConfig.dayStartMode)) + app.buildRow(`<span data-tooltip-html="${app.TOOLTIPS.WEEK_START}">Week Start</span>`, `<select id="init-week-start" class="bbgl-native-select"><option value="sun">Sun &ndash; Sat</option><option value="mon">Mon &ndash; Sun</option></select>`); const startBtn = app.buildButton('init-start-btn', 'START TRACKING', 'green', 'margin:8px 10px; width: calc(100% - 20px); display:block;'); const body = `<div id="init-section-masked-body" class="bbgl-mask-host" data-mask-text="Please agree to the privacy disclosure first.">${inputHTML}${createBtn}${rows}${startBtn}</div>`; return app.buildSection('Initialization Settings', body, 'margin-bottom:5px;'); }

function buildWelcomeReturningSection() { const note = `<div id="bbgl-welcome-returning-text">${app.DOC_LOADING_HTML}</div>`; const importBtn = app.buildButton('init-returning-import-btn', 'IMPORT LOG', '', 'margin:0 10px 8px 10px; width: calc(100% - 20px); display:block;'); const hiddenFile = `<input type="file" id="init-import-file" accept=".json,application/json" style="display:none">`; return app.buildSection('Returning User', note + importBtn + hiddenFile, 'margin-bottom:5px;'); }

app.generateDemoData = generateDemoData;
app.refreshDemoMasks = refreshDemoMasks;
app.enterDemo = enterDemo;
app.enterDemoFromSettings = enterDemoFromSettings;
app.buildWelcomeIntroSection = buildWelcomeIntroSection;
app.buildWelcomeInitSection = buildWelcomeInitSection;
app.buildWelcomeReturningSection = buildWelcomeReturningSection;
export { generateDemoData, refreshDemoMasks, enterDemo, enterDemoFromSettings, buildWelcomeIntroSection, buildWelcomeInitSection, buildWelcomeReturningSection };
