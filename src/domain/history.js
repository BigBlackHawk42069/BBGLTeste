import { app } from '../app-context.js';
import { CUSTOM_STICKERS, PAGE_TITLES, cdnize } from '../ui/assets.ts';
import { ASSETS, ICONS } from '../ui/icons.ts';
import {
  ACH_FMT, BACKFILL, BACKFILL_GROUP_KEYS, BACKFILL_GROUP_OF, BACKFILL_GROUPS,
  BASE_DOCS_URL, BBGL_ERROR_CODE, BS_STAT_ROWS, compareVersions, CONSTANTS, ECAN_LOG, ECSTASY_LOG, ENERGY_LOGS, ENERGY_PARAM,
  EX_OD_LOG, GAME, GYM_TIERS, HAPPY_LOGS, ITEM_GROUP_LABELS, ITEM_LOG_META, ITEM_LOGS, KEYS, LAYOUT, LSD_OD_LOG,
  MSG_CLIPBOARD_DENIED, MSG_KEY_FORMAT_INVALID, MSG_KEY_NETWORK_ERROR, MSG_SYNC_NETWORK_ERROR, MSG_SYNC_QUOTA,
  OD_LOGS, r2, SCRIPT_VERSION, STAT_ENHANCER_PARAM, STAT_HAPPY_PARAM, STAT_KEYS, STAT_LOGS, SYNC_FROM_BUFFER,
  TORN_KEY_ERROR_MAP, TRAIN_ENERGY_PARAM, TRAIN_LOGS, WIPE_BELOW_VERSION, XANAX_LOG, XANAX_OD_LOG, ZERO_BREAKDOWN,
  bbglError, tornKeyErrorText
} from '../core/constants.ts';
import { Log, Perf, isDevMode } from '../core/log.ts';
import {
  ALLOWED_CONFIG_KEYS, TAB_ID, calendarState, dom, graphState, historyCache, lastButtonLocation, layoutObservers,
  refreshClickLog, runtime, saveConfig, saveViewState, setHistoryCache, setLastButtonLocation, setTopCeiling, setViewState,
  topCeilingCache, topCeilingTs, userConfig, viewState
} from '../core/state.ts';
import { Formatter, TimeManager, getISOWeek, getWeekKey } from '../domain/time.ts';
import { classifyDay, computeWeekCapsules, computeWeekCompletion, placeCapsuleUnit } from '../domain/capsules.ts';
import { atrophyTitle, calculateLevelProgress, computeDailyLevelExp, computeLevelExpCost } from '../domain/leveling.ts';
import { findHappyJumps, initializeDayObject, normalizeApiLogs, sumStats } from '../domain/day.ts';
import { rebuildFromSeries, reconcileIncremental } from '../domain/history-engine.ts';


function getStickerState(id) { const states = historyCache && historyCache.meta && historyCache.meta.stickers ? historyCache.meta.stickers : {}; return states[String(id)] || '--'; }

async function persistStickerCleared(id) { try { const stored = await app.DBManager.getStorage(); if (!stored) return; if (!stored.meta) stored.meta = {}; if (!stored.meta.stickers) stored.meta.stickers = {}; const key = String(id); const cachedState = historyCache && historyCache.meta && historyCache.meta.stickers && historyCache.meta.stickers[key] || '--'; const newState = cachedState[0] + '+'; stored.meta.stickers[key] = newState; await app.DBManager.setStorage(stored); if (historyCache) { if (!historyCache.meta) historyCache.meta = {}; if (!historyCache.meta.stickers) historyCache.meta.stickers = {}; historyCache.meta.stickers[key] = newState; } } catch (e) { Log.warn('Failed to persist sticker cleared state', e); } }

function getInstallWeekKey() { const rewardStartDate = getActiveHistory().meta.rewardStartDate; if (!rewardStartDate) return null; return getWeekKey(Formatter.dateLogical(rewardStartDate * 1000)); }

function getInstallDateKey() { const rewardStartDate = getActiveHistory().meta.rewardStartDate; if (!rewardStartDate) return null; return Formatter.dateLogical(rewardStartDate * 1000); }

const DataController = { _cache: { timeline: null, slices: {}, dateMap: null, rateArr: null, stickerMap: null, unlockedCount: null, featuredDays: null, hjData: null }, invalidate() { this._cache.timeline = null; this._cache.slices = {}; this._cache.dateMap = null; this._cache.rateArr = null; this._cache.stickerMap = null; this._cache.unlockedCount = null; this._cache.featuredDays = null; this._cache.hjData = null; runtime.stickerData = []; runtime._achCache = null; }, invalidateToday() { this._cache.timeline = null; this._cache.dateMap = null; this._cache.slices = {}; }, hydrate(loaded) { setHistoryCache(loaded || null); this.invalidate(); }, syncCache(stored) { Perf.start('syncCache'); if (stored) { const clean = app.sanitizeStorageRecord(stored); const rebuilt = this._rebuildFromSeries(clean.series || [], clean.meta && clean.meta.baselineBreakdown || ZERO_BREAKDOWN); setHistoryCache({ meta: clean.meta || {}, history: rebuilt.history, today: rebuilt.today }); } else { setHistoryCache(null); } this.invalidate(); Perf.end('syncCache'); }, isStickerCleared(id) { if (runtime.demoMode) return id === 1; return getStickerState(id)[1] === '+'; }, markStickerCleared(id) { if (runtime.demoMode) return; persistStickerCleared(id); }, getHappyJumpData() { if (this._cache.hjData) return this._cache.hjData; const hjDaySet = new Set(); this.getTimeline().forEach(day => { findHappyJumps(day.series).forEach(jump => hjDaySet.add(jump.date)); }); this._cache.hjData = { hjDaySet }; return this._cache.hjData; }, buildProgressionCache() { if (this._cache.stickerMap) return; const today = Formatter.dateLogical(); const todayWeekKey = getWeekKey(today); const weekMap = {}; this.getTimeline().forEach(day => { if (day.date >= today) return; const wk = getWeekKey(day.date); if (!weekMap[wk]) weekMap[wk] = []; weekMap[wk].push(day); }); const { hjDaySet } = this.getHappyJumpData(); const stickerMap = new Map(); const featuredSet = new Set(); let unlockedCount = 1; let rouletteCounter = 0; let careerLevelExp = 0; const installWeekKey = runtime.demoMode ? null : getInstallWeekKey(); const installDateKey = runtime.demoMode ? null : getInstallDateKey(); const rewardStartTs = runtime.demoMode ? null : getActiveHistory().meta && getActiveHistory().meta.rewardStartDate || null; Object.keys(weekMap).sort().forEach(wk => { if (installWeekKey && wk < installWeekKey) return; const days = weekMap[wk].sort((a, b) => a.date.localeCompare(b.date)); if (!runtime.demoMode) { days.forEach(day => { if (installDateKey && day.date < installDateKey) return; let daySeries = day.series || []; if (installDateKey && day.date === installDateKey && rewardStartTs) { daySeries = daySeries.filter(s => s.ts >= rewardStartTs); } const e = daySeries.filter(s => s.type === 'gym').reduce((sum, s) => sum + (s.cost || 0), 0); const hasTrainLog = daySeries.some(s => s.type === 'gym'); const isHJ = daySeries === day.series ? hjDaySet.has(day.date) : findHappyJumps(daySeries).length > 0; careerLevelExp += computeDailyLevelExp(e, hasTrainLog, isHJ); }); } if (wk >= todayWeekKey) return; const stickerworthyDays = days.filter(d => d.eSpent && d.eSpent.total >= 1000); if (!stickerworthyDays.length) return; const { isCompleted, isGold, isDiamond } = computeWeekCompletion(days, hjDaySet); const numFeatured = isGold ? 2 : isCompleted ? 1 : 0; const splitIdx = Math.max(0, stickerworthyDays.length - numFeatured); const rouletteDays = stickerworthyDays.slice(0, splitIdx); const featuredDays = stickerworthyDays.slice(splitIdx); const rouletteStep = unlockedCount <= 20 && unlockedCount !== 11 ? 11 : 9; rouletteDays.forEach(day => { const rawIdx = rouletteCounter * rouletteStep % unlockedCount; const idx = runtime.demoMode ? 0 : rawIdx; stickerMap.set(day.date, CUSTOM_STICKERS[idx]); rouletteCounter++; }); featuredDays.forEach((day, i) => { const newIdx = unlockedCount + i; if (newIdx < CUSTOM_STICKERS.length) { const idx = runtime.demoMode ? 0 : newIdx; stickerMap.set(day.date, CUSTOM_STICKERS[idx]); featuredSet.add(day.date); } else { const rawIdx = rouletteCounter * rouletteStep % unlockedCount; const idx = runtime.demoMode ? 0 : rawIdx; stickerMap.set(day.date, CUSTOM_STICKERS[idx]); rouletteCounter++; } }); unlockedCount = Math.min(unlockedCount + numFeatured, CUSTOM_STICKERS.length); }); if (runtime.demoMode) unlockedCount = 1; this._cache.stickerMap = stickerMap; this._cache.featuredDays = featuredSet; this._cache.unlockedCount = unlockedCount; runtime.careerLevelExp = careerLevelExp; if (!runtime.demoMode) { const existingStates = historyCache && historyCache.meta && historyCache.meta.stickers ? historyCache.meta.stickers : {}; const freshStates = {}; for (let i = 1; i <= CUSTOM_STICKERS.length; i++) { const key = String(i); const wasClear = (existingStates[key] || '--')[1] === '+'; freshStates[key] = (i <= unlockedCount ? '+' : '-') + (wasClear ? '+' : '-'); } if (historyCache) { if (!historyCache.meta) historyCache.meta = {}; historyCache.meta.stickers = freshStates; } } },
    getStickerMap() { this.buildProgressionCache(); return this._cache.stickerMap; }, getCareerLevelExp() { this.buildProgressionCache(); return runtime.careerLevelExp || 0; }, getUnlockedCount() { this.buildProgressionCache(); return this._cache.unlockedCount || 0; }, getFeaturedDays() { this.buildProgressionCache(); return this._cache.featuredDays; }, getTimeline() { if (this._cache.timeline) return this._cache.timeline; const s = getActiveHistory(); let t = [...(s.history || [])]; if (s.today && (s.today.date || s.today.startTotal > 0)) { t = t.filter(d => d.date !== s.today.date); t.push(s.today); } t.sort((a, b) => a.date.localeCompare(b.date)); if (s.meta && s.meta.logStartDate) { const floor = Formatter.dateLogical(s.meta.logStartDate * 1000); t = t.filter(d => d.date >= floor); } this._cache.timeline = t; return t; }, getDateMap() { if (this._cache.dateMap) return this._cache.dateMap; const t = this.getTimeline(), m = {}; t.forEach(d => { m[d.date] = d; }); this._cache.dateMap = m; return m; }, periodCalendarDays(sl) { if (!sl || sl.resolution === 'DAY') return 1; const DAY = 86400000, today = Formatter.dateLogical(); const span = (startStr, endStr) => { const end = endStr > today ? today : endStr; if (!startStr || !end) return 1; return Math.max(1, Math.round((Formatter.parse(end).getTime() - Formatter.parse(startStr).getTime()) / DAY) + 1); }; const dl = sl._dailyList || []; if (sl.resolution === 'WEEK') { const s = sl._weekStart || dl[0] && dl[0].date, e = sl._weekEnd || (dl.length ? dl[dl.length - 1].date : null); return s && e ? span(s, e) : dl.length || 1; } if (!dl.length) return 1; if (sl.resolution === 'MONTH') { const p = dl[0].date.slice(0, 7), y = +p.slice(0, 4), mo = +p.slice(5, 7); const dim = new Date(y, mo, 0).getDate(); return span(`${p}-01`, `${p}-${String(dim).padStart(2, '0')}`); } if (sl.resolution === 'YEAR') { const y = dl[0].date.slice(0, 4); return span(`${y}-01-01`, `${y}-12-31`); } const tl = this.getTimeline(); return tl.length ? span(tl[0].date, today) : dl.length || 1; }, _buildRateCache() { const h = getActiveHistory(); const allDays = [...(h.history || [])].sort((a, b) => a.date.localeCompare(b.date)); const running = { str: null, def: null, spd: null, dex: null }; const arr = []; const derived = {}; let floorDate = null; if (h.meta && h.meta.logStartDate) { floorDate = Formatter.dateLogical(h.meta.logStartDate * 1000); } allDays.forEach(day => { if (day.series && day.series.length > 0) { day.series.forEach(e => { if (e.cost > 0) { running[e.stat] = e.rate; if (!derived[e.stat] && (!floorDate || day.date >= floorDate)) { derived[e.stat] = e.rate; } } }); } else { STAT_KEYS.forEach(k => { const cost = day.eSpent && day.eSpent[k] || 0; const gain = day.gains ? day.gains[k] || 0 : 0; if (cost > 0) { running[k] = gain / cost * 150; if (!derived[k] && (!floorDate || day.date >= floorDate)) { derived[k] = gain / cost * 150; } } }); } arr.push({ date: day.date, rates: { ...running } }); }); this._cache.rateArr = arr; this._cache.originRates = derived; }, getHistoricalRate(dateStr, stat) { if (!this._cache.rateArr) this._buildRateCache(); const arr = this._cache.rateArr, or = this._cache.originRates; let lo = 0, hi = arr.length - 1, best = -1; while (lo <= hi) { const mid = lo + hi >> 1; if (arr[mid].date <= dateStr) { best = mid; lo = mid + 1; } else hi = mid - 1; } if (best === -1) return or[stat] || 0; const rate = arr[best].rates[stat]; return rate !== null ? rate : or[stat] || 0; }, getOriginRate(stat) { if (!this._cache.rateArr) this._buildRateCache(); return this._cache.originRates && this._cache.originRates[stat] || 0; }, getSlice(mode, target, year = null) { let k = `${mode}_${target}`; if (mode === 'CUSTOM') k = `CUSTOM_${target.map(d => d.date).join('_')}`; if (mode === 'MONTH') k = `MONTH_${year}_${target}`; if (this._cache.slices[k]) return this._cache.slices[k]; let raw = null, res = mode, list = []; if (mode === 'DAY') raw = this.getDateMap()[target];else if (mode === 'MONTH') { const idx = CONSTANTS.MONTHS.indexOf(target); if (idx > -1) { const p = `${year}-${String(idx + 1).padStart(2, '0')}`; list = this.getTimeline().filter(d => d.date.startsWith(p)); } } else if (mode === 'YEAR') list = this.getTimeline().filter(d => d.date.startsWith(target));else if (mode === 'ALL') { list = this.getTimeline(); res = 'ALL'; } else if (mode === 'CUSTOM') { list = target; res = 'WEEK'; } const sl = this._hydrate(raw, list, target, res); this._cache.slices[k] = sl; return sl; }, _getLastEntryRate(day, stat, totalGain, totalCost) { if (day.series && day.series.length > 0) { for (let i = day.series.length - 1; i >= 0; i--) { const entry = day.series[i]; if (entry.stat === stat && entry.cost > 0) { return entry.rate != null ? entry.rate : r2(entry.gain / entry.cost * 150); } } } return r2(totalGain / totalCost * 150); }, _hydrate(sDay, dList, lbl, res) { const r = { label: lbl, resolution: res, date: sDay ? sDay.date : dList[0] ? dList[0].date : lbl, stats: {}, meta: { tier: 0,
    isGap: false, totalEnergy: 0 }, _dailyList: dList || [] }; const ge = (d, k) => !d || !d.eSpent ? 0 : d.eSpent[k] || 0; const gg = (d, k) => d && d.gains ? d.gains[k] || 0 : 0; const gend = (d, k) => d && (d.endBreakdown || d.end) ? (d.endBreakdown || d.end)[k] || 0 : 0; const gst = (d, k) => d && (d.startBreakdown || d.start) ? (d.startBreakdown || d.start)[k] || 0 : 0; const keys = [...STAT_KEYS, 'total']; if (sDay) { keys.forEach(k => { const e = ge(sDay, k), g = gg(sDay, k); let s = gst(sDay, k), end = gend(sDay, k); if (k === 'total') { if (!s) s = STAT_KEYS.reduce((a, x) => a + gst(sDay, x), 0); if (!end) end = STAT_KEYS.reduce((a, x) => a + gend(sDay, x), 0); } r.stats[k] = { start: s, gain: g, end: end, cost: e, rate: e > 0 ? this._getLastEntryRate(sDay, k, g, e) : k !== 'total' ? this.getHistoricalRate(sDay.date, k) : 0 }; }); r.meta.totalEnergy = r.stats.total.cost; } else if (dList.length > 0) { const srt = [...dList].sort((a, b) => a.date.localeCompare(b.date)), f = srt[0], l = srt[srt.length - 1]; keys.forEach(k => { let tc = 0, tg = 0; srt.forEach(d => { tc += ge(d, k); tg += gg(d, k); }); let s = gst(f, k), end = gend(l, k); if (k === 'total') { if (!s) s = STAT_KEYS.reduce((a, x) => a + gst(f, x), 0); if (!end) end = STAT_KEYS.reduce((a, x) => a + gend(l, x), 0); } r.stats[k] = { start: s, gain: tg, end: end, cost: tc, rate: tc > 0 ? r2(tg / tc * 150) : 0 }; }); r.meta.totalEnergy = r.stats.total.cost; } else { r.meta.isGap = true; const pastEnd = { ...([...this.getTimeline()].reverse().find(d => d.date < r.date)?.endBreakdown || getActiveHistory().meta.baselineBreakdown || {}) }; pastEnd.total = STAT_KEYS.reduce((a, x) => a + (pastEnd[x] || 0), 0); keys.forEach(k => { r.stats[k] = { start: pastEnd[k] || 0, gain: 0, end: pastEnd[k] || 0, cost: 0, rate: k !== 'total' ? this.getHistoricalRate(r.date, k) : 0 }; }); } keys.forEach(k => { if (r.stats[k]) r.stats[k].gain = Math.max(0, r2(r.stats[k].end - r.stats[k].start)); }); const e = r.meta.totalEnergy; let hjDaySet; if (this.getHappyJumpData) { const hjData = this.getHappyJumpData(); hjDaySet = hjData.hjDaySet; } else { hjDaySet = new Set(); } const isHJ = r.date && hjDaySet.has(r.date); if (e >= 2000) r.meta.tier = 3;else if (e >= 1500) r.meta.tier = 2;else if (e >= 1000 || isHJ) r.meta.tier = 1;else r.meta.tier = 0; const itemDays = sDay ? [sDay] : dList || []; const items = {}; let itemEnergy = 0; let odEnergyLost = 0; let odHappyLost = 0; itemDays.forEach(d => { if (d && d.items) Object.keys(d.items).forEach(id => { items[id] = (items[id] || 0) + d.items[id]; }); (d && d.series || []).forEach(e => { if (e.type !== 'item') return; if (e.logId === ECAN_LOG && e.energy) itemEnergy += e.energy; if (e.energyLost != null) odEnergyLost += e.energyLost; if (e.happyLost != null) odHappyLost += e.happyLost; }); }); r.items = items; r.xanax = items[XANAX_LOG] || 0; r.xanaxODs = items[XANAX_OD_LOG] || 0; r.lsdODs = items[LSD_OD_LOG] || 0; r.exODs = items[EX_OD_LOG] || 0; r.odEnergyLost = odEnergyLost; r.exHappyLost = odHappyLost; r.ecans = items[ECAN_LOG] || 0; r.ecanEnergy = itemEnergy; r.dayCount = sDay ? 1 : dList ? dList.length : 0; return r; }, async processDataPayload(apiLogs, apiBattlestats) { Perf.start('processDataPayload'); let s = getActiveHistory(); const fullApiLogs = normalizeApiLogs(apiLogs); let cleanLogs = fullApiLogs; if (s.meta.logStartDate) { cleanLogs = cleanLogs.filter(l => l.ts >= s.meta.logStartDate); if (cleanLogs.length === 0 && historyCache) { const changedToday = apiBattlestats ? this._snapToBattlestats(apiBattlestats, s) : false; const logicalToday = Formatter.dateLogical(); if (s.today.date !== logicalToday) { const changedDays = []; if (s.today.series && s.today.series.length > 0 || s.today.gains && s.today.gains.total > 0) { s.history.push(s.today); changedDays.push(s.today); } s.today = initializeDayObject(logicalToday, s.today.endBreakdown); changedDays.push(s.today); setHistoryCache(s); this.invalidate(); await app.DBManager.saveDays(s.meta, changedDays); } else if (changedToday) { setHistoryCache(s); this.invalidateToday(); await app.DBManager.saveDays(s.meta, [s.today]); } window.dispatchEvent(new CustomEvent('bbgl:dataUpdated')); Perf.end('processDataPayload'); return 'SUCCESS'; } let inc = null; try { inc = this._reconcileIncremental(s, cleanLogs); } catch (e) { Log.warn('Incremental reconcile failed; falling back to full rebuild', e); inc = null; } if (inc) { setHistoryCache(inc.result); s = getActiveHistory(); this._runDailyGrind([], apiBattlestats, s); const changedDays = inc.changedDays.slice(); const logicalToday = Formatter.dateLogical(); let rolled = false; if (s.today.date !== logicalToday) { if (s.today.series && s.today.series.length > 0 || s.today.gains && s.today.gains.total > 0) s.history.push(s.today); s.today = initializeDayObject(logicalToday,
    s.today.endBreakdown); rolled = true; } setHistoryCache(s); this.invalidate(); if (rolled) { const all = [...(s.history || [])]; if (s.today) all.push(s.today); await app.DBManager.saveDays(s.meta, all); } else { if (!changedDays.includes(s.today)) changedDays.push(s.today); await app.DBManager.saveDays(s.meta, changedDays); } window.dispatchEvent(new CustomEvent('bbgl:dataUpdated')); Perf.end('processDataPayload'); return 'SUCCESS'; } try { setHistoryCache(await this._reconcileFull(s, cleanLogs)); } catch (e) { Log.warn('Reconciliation error', e); } s = getActiveHistory(); } if (!s.meta.logStartDate) { if (apiBattlestats) { s.meta.baselineBreakdown = { str: apiBattlestats.strength || 0, def: apiBattlestats.defense || 0, spd: apiBattlestats.speed || 0, dex: apiBattlestats.dexterity || 0 }; } const nowTs = Math.floor(Date.now() / 1000); s.meta.logStartDate = nowTs; s.meta.rewardStartDate = nowTs; cleanLogs = cleanLogs.filter(l => l.ts >= s.meta.logStartDate); s.today = initializeDayObject(Formatter.dateLogical(), { ...s.meta.baselineBreakdown }); } this._runDailyGrind(cleanLogs, apiBattlestats, s); const logicalToday = Formatter.dateLogical(); if (s.today.date !== logicalToday) { if (s.today.series && s.today.series.length > 0 || s.today.gains && s.today.gains.total > 0) s.history.push(s.today); s.today = initializeDayObject(logicalToday, s.today.endBreakdown); } this.saveSmartHistory(s); window.dispatchEvent(new CustomEvent('bbgl:dataUpdated')); Perf.end('processDataPayload'); return 'SUCCESS'; }, saveSmartHistory(d) { const allDays = [...(d.history || [])]; if (d.today) allDays.push(d.today); app.DBManager.saveDays(d.meta, allDays); setHistoryCache(d); this.invalidate(); }, flattenAllSeries() { const s = getActiveHistory(); const all = []; const days = [...(s.history || [])]; if (s.today) days.push(s.today); days.forEach(day => { if (day.series && day.series.length > 0) { day.series.forEach(e => all.push(e)); } else { const base = Formatter.parse(day.date); const ts = Math.floor(base.getTime() / 1000) + 43200; STAT_KEYS.forEach(stat => { const gain = day.gains && day.gains[stat] || 0; const cost = day.eSpent && day.eSpent[stat] || 0; const after = day.endBreakdown && day.endBreakdown[stat] || 0; if (gain > 0 || cost > 0) all.push({ ts, stat, gain, cost, after, rate: cost > 0 ? r2(gain / cost * 150) : 0, synthetic: true }); }); } }); return all.sort((a, b) => a.ts - b.ts); }, _runDailyGrind(logs, bs, s) { const allDays = [...(s.history || []), s.today]; const globalLastTs = allDays.reduce((max, day) => Math.max(max, day.lastLogTimestamp || 0), 0); const lastTs = Math.max(globalLastTs, s.meta.logStartDate || 0); const validLogs = logs.filter(l => l.ts > lastTs); validLogs.forEach(l => this._applyLogToState(l, s)); if (bs) this._snapToBattlestats(bs, s); }, _applyLogToState(l, s) { const logDate = Formatter.dateLogical(l.ts * 1000); if (s.today.date !== logDate) { if (s.today.series && s.today.series.length > 0) s.history.push(s.today); s.today = initializeDayObject(logDate, s.today.endBreakdown); } if (l.type === 'item') { if (!s.today.items) s.today.items = {}; if (!s.today.itemLogIds) s.today.itemLogIds = []; const itemKey = `${l.ts}_${l.logId}`; if (!s.today.itemLogIds.includes(itemKey)) { s.today.itemLogIds.push(itemKey); s.today.items[l.logId] = (s.today.items[l.logId] || 0) + 1; if (l.logId === ECAN_LOG && l.energy) s.today.itemEnergy = (s.today.itemEnergy || 0) + l.energy; if (l.energyLost != null) s.today.itemEnergyLost = (s.today.itemEnergyLost || 0) + l.energyLost; if (l.happyLost != null) s.today.itemHappyLost = (s.today.itemHappyLost || 0) + l.happyLost; if (l.happy) s.today.itemHappy = (s.today.itemHappy || 0) + l.happy; } const entry = { type: 'item', id: l.id, ts: l.ts, logId: l.logId }; if (l.energy) entry.energy = l.energy; if (l.energyLost != null) entry.energyLost = l.energyLost; if (l.happyLost != null) entry.happyLost = l.happyLost; if (l.happy) entry.happy = l.happy; if (l.statKey) { entry.statKey = l.statKey; entry.statGain = l.statGain; } s.today.series.push(entry); } else { s.today.gains[l.stat] += l.gain; s.today.gains.total += l.gain; s.today.eSpent[l.stat] += l.cost; s.today.eSpent.total += l.cost; s.today.endBreakdown[l.stat] = l.after; if (l.ts > s.today.lastLogTimestamp) s.today.lastLogTimestamp = l.ts; s.today.series.push({ type: 'gym', id: l.id, ts: l.ts, stat: l.stat, gain: l.gain, cost: l.cost, after: l.after, rate: l.cost > 0 ? r2(l.gain / l.cost * 150) : 0 }); s.today.endTotal = sumStats(s.today.endBreakdown); } }, _snapToBattlestats(bs,
    s) { let upd = false; BS_STAT_ROWS.forEach(i => { const apiVal = bs[i.api]; if (apiVal === undefined) return; const localVal = s.today.endBreakdown[i.abbr] || 0; const lg = s.today.gains[i.abbr] || 0; if (localVal !== apiVal) { s.today.endBreakdown[i.abbr] = apiVal; s.today.startBreakdown[i.abbr] = apiVal - lg; upd = true; } }); if (upd) { s.today.endTotal = sumStats(s.today.endBreakdown); s.today.startTotal = sumStats(s.today.startBreakdown); } return upd; }, _rebuildFromSeries(seriesArr, baselineBreakdown) { Perf.start('_rebuildFromSeries'); const rebuilt = rebuildFromSeries(seriesArr, baselineBreakdown); Perf.end('_rebuildFromSeries'); return rebuilt; }, async _reconcileFull(s, cleanLogs) { const stored = await app.DBManager.getStorage(); if (!stored) return { meta: s.meta, history: s.history, today: s.today }; if (stored.series && cleanLogs.length > 0) { const minApiTs = cleanLogs[0].ts; const maxApiTs = cleanLogs[cleanLogs.length - 1].ts; const apiEntries = cleanLogs.map(l => { if (l.type === 'item') return { ...l }; return { type: 'gym', id: l.id, ts: l.ts, stat: l.stat, gain: r2(l.gain), cost: l.cost, after: r2(l.after) }; }); const getSetKey = e => e.type === 'item' ? `item_${e.id}` : `${e.ts}_${e.stat}_${e.after}`; const apiTsStatSet = new Set(apiEntries.map(getSetKey)); const kept = stored.series.filter(e => e.ts < minApiTs || e.ts > maxApiTs || !apiTsStatSet.has(getSetKey(e))); stored.series = [...kept, ...apiEntries].sort((a, b) => a.ts - b.ts); } stored.meta = { ...stored.meta, logStartDate: s.meta.logStartDate, syncFloor: s.meta.syncFloor || stored.meta.syncFloor, stickers: stored.meta.stickers || s.meta.stickers || {} }; const rebuilt = this._rebuildFromSeries(stored.series || [], stored.meta.baselineBreakdown || ZERO_BREAKDOWN); return { meta: stored.meta, history: rebuilt.history, today: rebuilt.today }; }, _reconcileIncremental(s, cleanLogs) { return reconcileIncremental(s, cleanLogs); } };

function getActiveHistory() { if (runtime.demoMode) { if (!runtime.demoHistory) runtime.demoHistory = app.generateDemoData(); return runtime.demoHistory; } if (historyCache) return historyCache; return { meta: { baselineBreakdown: { ...ZERO_BREAKDOWN }, backfill: app.defaultBackfill() }, history: [], today: initializeDayObject(Formatter.dateLogical(), { ...ZERO_BREAKDOWN }) }; }

app.getStickerState = getStickerState;
app.persistStickerCleared = persistStickerCleared;
app.getInstallWeekKey = getInstallWeekKey;
app.getInstallDateKey = getInstallDateKey;
app.DataController = DataController;
app.getActiveHistory = getActiveHistory;
export { getStickerState, persistStickerCleared, getInstallWeekKey, getInstallDateKey, DataController, getActiveHistory };
